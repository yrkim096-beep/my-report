# CLAUDE.md

이 문서는 이 프로젝트에서 이미 정한 것만 적는다. 정하지 않은 항목은 "미정"으로 남긴다.
지어내지 않는다 — 새 값이 필요하면 `core/config.py`를 먼저 고치고 이 문서를 갱신한다.

## 이 앱이 무엇인가

임대 매물 리드가 문의 접수부터 첫 월세 납부까지 전환되는 과정을 추적하는 성장 리포트 앱.
(`core/config.py` — `DATASET = "rental_lead_funnel"`)

## 그레인: 무엇을 하나로 세는가

- **획득 퍼널**: 리드(`lead_id`) 1건. 한 리드가 같은 단계를 두 번 밟지 않는다
  (leads.csv 그레인 노트: 재문의는 별도 lead_id로 새 행). 각 단계에 "도달한 lead_id 집합"을
  먼저 만들고 집합 크기를 세어, 팬아웃(한 리드에 여러 상담·방문 로그)이 있어도 중복으로
  세지 않는다. (`core/metrics.py` — `funnel()`)
- **유지 퍼널**: 과제(계약, `contract_id`) 1건. 이 데이터엔 "과제"·"집행" 테이블이 따로
  없어 계약을 과제로, 그 계약의 월세 납부(`rent_payments`에서 `paid == True`)를 집행으로
  놓았다. 단계는 착수(첫 집행 기록 있음) → 지속(착수 후 90일 안에 집행이 한 번 더 있음)
  → 완주(종료 상태로 마감). "지속" 판정만 예외적으로 대상×시간(첫 집행과 두 번째 집행
  사이 간격)을 본다 — 착수·완주는 시점 하나의 상태다.
  (`core/config.py` — `RETENTION_STEPS`, `core/metrics.py` — `retention_funnel()`)
- **유지·이탈의 정의**: 유지 = `status == "재계약"`, 이탈 = `status`가 `만료_미갱신` 또는
  `중도해지`. `계약중`(아직 기간이 안 끝난 계약)은 재계약 여부가 정해지지 않았으므로
  유지도 이탈도 아니다 — 판정 대상에서 뺀다. 다만 계약중인 것 중 마지막 집행 후
  `config.CHURN_INACTIVITY_DAYS`(180일) 이상 무활동인 것은 "위험 표시"만 남긴다(이탈로
  세지는 않는다). 기준 시점은 데이터에 남은 최신 값이 아니라 `config.PERIOD` 종료일이다 —
  최신 값을 쓰면 표본 1건짜리 꼬리에 흔들린다. (`core/metrics.py` — `churn()`)

## 퍼널 단계와 각 단계의 조건

순서: 문의접수 → 상담진행 → 방문예약진행 → 계약체결 → 입주 → 첫월세납부
(`core/config.py` — `FUNNEL_STEPS`)

| 단계 | 조건 |
|---|---|
| 문의접수 | `leads` 테이블에 존재하는 lead_id 전체 |
| 상담진행 | `consultations` 테이블에 존재하는 lead_id |
| 방문예약진행 | `visits` 테이블에 존재하는 lead_id |
| 계약체결 | `contracts` 테이블에 존재하는 lead_id |
| 입주 | `contracts` 테이블에 존재하는 lead_id (계약체결과 동일 집합) |
| 첫월세납부 | `rent_payments`에서 `paid == True`인 계약의 lead_id |

**알려진 한계** (`core/metrics.py` — `funnel()` 주석): "계약체결"과 "입주"는 이 데이터에서
같은 사건이다. `contracts.contract_start_date` 하나만 있고 별도 입주일 컬럼이 없어서 두
단계의 lead_id 집합이 동일하다 — 전환율 100%가 뜨는 게 정상이다. 실제 데이터를 반입하면
입주일이 따로 있는지 다시 확인해야 한다.

## 지표 이름과 계산식

퍼널 전환 지표, KPI 카드, 기간별 추이, 유지 퍼널 지표, 분해, 전후 비교까지 정해졌다.
채널 효율만 미정(선택 과제).

| 지표 | 계산식 |
|---|---|
| step_rate | n / 전 단계 n (첫 단계는 NaN) |
| cum_rate | n / 첫 단계 n |
| drop | 전 단계 n − 현재 단계 n |
| is_bottleneck | step_rate가 가장 낮은 구간에 True |

- KPI 카드(`kpis()`): 규모·전환·속도·품질 네 축에서 하나씩 —
  리드 수(leads 전체 건수) · 전환율(계약까지 간 리드 비율) ·
  체결소요일(문의 접수~계약 체결까지 소요일수 중앙값) · 납부이행률(정상 납부 비율)
- 기간별 추이(`monthly()`): 위 네 지표를 월별로 쪼갠 것. 열 이름이 `kpis()`의 지표 이름과
  같아야 스파크라인이 그려진다. ★ 최근 달은 아직 계약이 안 된 리드가 섞여 전환율이 낮게
  집계될 수 있음(관측 기간 문제 — 성과 문제 아님)
- 유지 퍼널 지표(`retention_funnel()`): 착수·지속·완주 세 단계의 n·step_rate·cum_rate
  (정의는 위 "그레인" 절 참고)
- 분해(`funnel_by()`): 담당자·유입채널 두 축만 쓴다(`DIM_SOURCE`) — 매물유형은 격차가
  2.2%p뿐이라 뺐다. 반환에 `band`·`믿음`·`가림사유`·`표시`가 있어 표본이 적은 칸은 전환율
  대신 사유만 보여준다(아래 "못 믿을 조건과 판정 순서" 참고)
- 전후 비교(`before_after()`): 이 프로젝트엔 실험(A/B)이 없어 대신 만들었다. 기준월
  이전/이후로 나눠 `kpis()`를 각각 계산한다(판정 기준은 아래 절 참고)
- 채널 효율(`channel_efficiency()`): 미정 (선택 과제)
- 상태 판정 규칙(`status_of()`): 도메인과 무관하게 고정된 규칙 — 값이 임계값의 "위험"보다
  나쁘면 block, "경고"보다 나쁘면 warn, 아니면 ok. `HIGHER_IS_WORSE`에 든 지표(체결소요일 등)는
  방향이 반대로 판정된다. (임계값 자체는 아래 참고)

## 임계값과 그 근거

| 항목 | 값 | 근거 |
|---|---|---|
| `THRESHOLDS` — 체결소요일 | 경고 10일 / 위험 20일 | 이보다 늘어나면 계약 시작(=월세 발생 시점)이 밀려 들어오는 월세 수입이 줄어든다 |
| `MIN_SAMPLE` (최상위 테이블 최소 표본) | 1,000 | 미정 — 코드에 값만 있고 근거는 문서화되지 않음 |
| `DECOMPOSE_MIN_SAMPLE` (분해 칸 최소 표본) | 50 | 방문예약진행→계약체결 구간(404명)의 실제 칸 크기(20~192건)를 보고 20/30/50 후보 중 골랐다. 12칸 중 5칸이 가려지되, 남는 칸(50건 이상)은 확실히 믿을 수 있다는 뜻이기도 하다 |

`THRESHOLDS`에 없는 지표는 전부 "정상(ok)"으로 표시된다. 전후 비교(`before_after()`)의
판정 기준(주지표 이동폭·가드레일 악화폭)은 "못 믿을 조건과 판정 순서" 절에 있다.

## 코드를 고칠 때 지킬 것

- 지표 정의는 `config.py`에서만 바꾼다.
- 계산 경로에 현재 시각을 넣지 않는다.
- 시키지 않은 파일은 고치지 않는다.

## 못 믿을 조건과 판정 순서

- **못 믿을 조건은 하나다 — 표본이 `config.DECOMPOSE_MIN_SAMPLE`(50건) 미만.**
  이 프로젝트엔 실험(A/B)이 없어 배정 공정성 조건은 해당 없음이고, 기간 미달 조건도
  따로 게이트로 만들지 않았다 — 조건이 하나뿐이어도 된다, 억지로 셋을 채우지 않는다.
  관측 기간이 대상마다 다른 문제는 게이트가 아니라 유지 퍼널·전후 비교의 "알려진
  한계"로만 남겨뒀다. (`core/metrics.py` — `trust_check()`, `funnel_by()`,
  `before_after()`가 전부 이 값을 같이 쓴다)
- **판정 순서: 못 믿을 조건 → 주지표 → 가드레일.** 못 믿을 조건에 걸리면 그 자리에서
  끝나고 주지표·가드레일은 아예 확인하지 않는다. 주지표가 안 움직였으면 가드레일도
  보지 않는다 — 가드레일로 성공을 뒤집는 건 주지표를 확인한 다음이다.
  (`core/metrics.py` — `before_after()`: 1) 표본 미달 → "판정 보류"
  2) 주지표(`전환율`)가 `BEFORE_AFTER_MOVE`(3%p) 미만 → "효과 없음"
  3) 가드레일(`납부이행률`)이 `BEFORE_AFTER_GUARD_WORSEN`(3%p) 이상 나빠짐 →
  "주의 필요" 4) 다 통과 → "성공")
- **못 믿을 때는 계산하지 않는다 — 계산한 뒤 숨기는 게 아니다.**
  `funnel_by()`는 표본 미달 칸의 `전환율`을 애초에 `NaN`으로 두고, `before_after()`는
  표본 미달이면 KPI 비교 자체를 하지 않고 "판정 보류"로 끝낸다.
- **감춘 항목에는 사유만 적고 수치는 적지 않는다.** 사유에는 실제 숫자를 넣는다 —
  "표본 부족"이 아니라 "표본 41건 (최소 50건)"처럼. 화면에서도 감춘 항목은 퍼센트·진행바
  대신 이 사유 문자열만 보여준다(`funnel_by()`의 `표시`/`가림사유`, `before_after()`의
  `사유`).

## 제안서 (`report/proposal.py`, `pages/5_제안서.py`) — 9주차 Day3 기준

리포트(`report/sections.py`)와 다른 문서다. 리포트는 "무슨 일이 있었는가", 제안서는
"그래서 무엇을 하자는가"를 적어 **결정을 요청**한다. 읽는 사람이 결정 권한을 가진
사람이라 계산 과정·함수 이름·컬럼 이름은 화면·문서 어디에도 넣지 않는다.
독립 메뉴(`pages/5_제안서.py`)에 있다 — 리포트 화면(`pages/3_리포트.py`)에는 붙이지 않는다
(9주차 Day2 때는 임시 탭으로 붙였었고, Day3에 독립 메뉴로 옮기며 탭은 제거했다).

- `report/proposal.py`의 `build(t, topic_key, cards, human)`은 **주제 하나**를 받아
  절 전체를 조립한다. 주제 후보는 `core.metrics.proposal_topics(t)`가 뽑고
  (퍼널 구간 · 분해 축 · 임계값 초과 · 추세 넷 중에서), 그중 하나를 사람이 고른다.
- 절 목록과 순서는 고정한다(질문형 제목): 한 장 요약 → 1.지금 어디서 새고 있습니까 →
  2.어느 지점에서 벌어집니까 → 3.얼마짜리 문제입니까 → 4.무엇을 하자는 것입니까 →
  5.이 판단이 틀릴 수 있는 지점(사람) → 6.무엇을 결정해 주셔야 합니까(사람, 선택지는 자동).
  최대 7절, 사람이 쓰는 절은 정확히 둘이다.
- 절을 새로 만들기 전에 "이 절을 채울 함수 이름을 지금 댈 수 있는가"부터 답한다.
  못 대면 그 절은 만들지 않는다(예: 실험 결과 · 경쟁사 비교 — 이 데이터엔 없음).
- 카드(`day2/제안카드.md`, 형식은 `제안.md` 참고)에 없는 값을 자동 절이 채워 넣지 않는다.
  없으면 "확인 필요"를 그대로 옮긴다. `cards_for_topic()`이 고른 주제(근거축)와 관련된
  카드만 걸러 보여준다 — 관련 카드가 없으면 빈 목록을 보여준다("이 주제에 대한 카드가
  아직 없다"), 상관없는 카드를 대신 보여주지 않는다.
- 문장 검사(`check_phrasing`)는 새로 만들지 않고 `report/sections.py` 것을 그대로
  import 해서 쓴다.
- 그래프(`viz/proposal_charts.py`)는 인라인 SVG 문자열이다. 색은 강조 1(`config.COLORS["block"]`)·
  대비 1(`config.COLORS["ok"]`)·기본 1(`config.BRAND["primary"]`)·회색 1, 총 넷을 안 넘는다.
  값이 없는 계열은 그리지 않는다(0으로 그리지 않는다).
- HTML 내보내기는 `제안서_템플릿.html`의 `<style>` 블록(A4 인쇄 규칙 포함)을 그대로
  읽어 재사용한다(`report/proposal.py` — `_extract_style()`). 새로 디자인하지 않는다.
- 한 장 요약의 제안 줄에는 카드의 되돌림 가능 여부를 바로 붙인다("(되돌림 가능/불가)").
  4번 절 카드 안에만 있으면 한 장 요약만 보는 사람은 놓친다(9주차 Day4 동료 지적으로 발견).
