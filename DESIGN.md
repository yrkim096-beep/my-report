# DESIGN.md — 성장 리포트 앱 설계 명세

**이 문서는 회고가 아니라 명세입니다.** "이렇게 만들었다"가 아니라 **"이렇게 만드십시오"** 입니다.
읽고 나서 자기 도메인으로 앱을 만들 수 있어야 합니다.

레퍼런스 구현: https://github.com/kym20b/project3-growth-report

| 절 | 내용 |
|---|---|
| 1 | 구조 — 화면을 왜 넷으로 나누는가 |
| 2 | 화면별 명세 |
| 3 | 디자인 시스템 |
| **4** | **내 도메인으로 바꾸기** ← 자기 주제로 옮길 때 여기부터 |
| 5 | 만드는 순서 |
| 6 | 배포 |
| 7 | 흔히 겪는 함정 |

---

# 1. 구조

## 1-1. 화면을 넷으로 나눈다

한 화면에 다 넣으면 **성격이 다른 것이 섞입니다.**

| 화면 | 언제 보는가 | 빈도 |
|---|---|---|
| **실행** | 새 데이터가 들어왔을 때 | 한 번 쓰고 끝 |
| **대시보드** | 성과를 볼 때 | **반복해서 계속** |
| **리포트** | 남에게 보낼 때 | 만들어 공유 |
| **아카이브** | 지난 것을 찾을 때 | 가끔 |

대시보드를 보려고 매번 8단계 스테퍼를 스크롤로 지나칠 이유가 없습니다.

## 1-2. 사람은 게이트에서만 개입한다

```
적재 → 프로파일 → 검증 → [게이트1] → 계산 → 대시보드 → 리포트 → [게이트3]
                            입구        ↑                          발송
                                    [게이트2]
                                      출구
```

| 게이트 | 위치 | 되돌림 | 묻는 것 |
|---|---|---|---|
| **1 입구** | 검증 후 | 가능 | 이 데이터로 분석을 시작해도 되는가 |
| **2 출구** | 계산 후 | 가능 | 계산 결과가 말이 되는가 |
| **3 발송** | 리포트 후 | **불가** | 이대로 내보내도 되는가 |

**앱은 판단하지 않습니다.** 판단할 재료를 놓고 사람이 누릅니다.
누가 언제 무엇을 보고 통과시켰는지 기록에 남습니다 — 이것이 거버넌스 기록입니다.

## 1-3. 앱이 하지 않는 것

- **지표를 스스로 정의하지 않습니다.** 정의는 위키가 원본이고 앱은 읽어 쓸 뿐입니다.
- **게이트를 대신 통과시키지 않습니다.**
- **원본 데이터를 고치지 않습니다.** 읽기만 합니다.
- **경고를 알아서 무시하지 않습니다.** 무시하면 사람은 무엇을 승인했는지 모른 채 승인합니다.

## 1-4. 파일 구조

```
app.py                 진입 · 사이드바 · 컨텍스트 바
pages/                 실행 · 대시보드 · 리포트 · 아카이브
core/
  config.py            ★ 도메인을 바꿀 때 고치는 곳은 여기 하나
  load.py              데이터 적재 — 모드가 갈리는 유일한 파일
  metrics.py           지표 계산
  validate.py          정합성 검증
  gates.py             게이트와 실행 기록
viz/
  ui.py                디자인 시스템 (색·간격은 여기서만 정의)
  charts.py            화면용 (Plotly)
  pdf_charts.py        인쇄용 (matplotlib)
report/
  sections.py          문서 8장 조립
  to_pdf.py            PDF
data/                  동봉 parquet
fonts/                 한글 폰트
tests/test_app.py      렌더 + 조작 점검
```

**규칙 하나: 계산 로직을 두 벌 만들지 마십시오.** 로컬과 배포에서 다른 코드가 돌면 반드시 어긋납니다.
갈리는 것은 `load.py` 하나뿐이고, 나머지는 어디서 왔든 같은 DataFrame을 받습니다.

---

# 2. 화면별 명세

## 2-1. 공통 — 상단 컨텍스트 바

**4개 화면 전부에 항상 있습니다.**

```
┌──────────────────────────────────────────────────────────┐
│ 데이터셋 project3_growth │ 기간 2025-01~12 │ 마지막 실행 … │ ● 완료 │
└──────────────────────────────────────────────────────────┘
```

화면을 옮겨도 **지금 보는 숫자가 어느 데이터의 것인지** 잃지 않게 합니다.
이것이 없으면 대시보드 숫자와 리포트 숫자가 다른 기간의 것인지 알 수 없습니다.

## 2-2. 실행

| 구역 | 요소 |
|---|---|
| 1 데이터 선택 | 등록된 데이터셋 / 파일 업로드 |
| 진행 | 8단계 스테퍼 (완료 ✓ · 현재 강조 · 미도달) |
| 2 프로파일 | 테이블별 행수·컬럼·기간·결측·메모리 |
| 3 검증 | 통과/경고/차단 개수 + 항목별 카드 |
| 게이트 1 | 경고 요약 · 판단 근거 입력 · 되돌리기/통과 |
| 4 계산 | 지표 카드 + 퍼널 표 |
| 게이트 2 | 판단 근거 · 되돌리기/통과 |
| 로그 | 시각 + 메시지 (터미널 스타일) |

**게이트 1은 경고를 요약해 보여줘야 합니다.** 항목 카드만 나열하면 사람이 안 읽습니다.

**차단이 있으면 통과 버튼을 비활성화합니다.** 경고는 통과할 수 있고, 차단은 못 합니다.

## 2-3. 대시보드 ★ 핵심

**여기서 발견이 일어납니다.** 가장 공들일 화면입니다.

```
[지표 카드 4개 + 스파크라인]
        ↓
[퍼널 6단계]            [기기별/채널별 분해 토글]
 병목 단계는 다른 색       격차를 문장으로 설명
        ↓
[실험 카드 N개]  판정이 곧 색
        ↓
[채널 효율]  CAC vs 유효CAC — 순위 역전이 보이게
        ↓
[개선 효과 계산기]  슬라이더 → 금액
```

### 지표 카드

```
┌──────────────┐
│ 전환율        │  ← 12px, muted
│ 3.72%        │  ← 32px, 700, tabular-nums
│ ▁▂▃▂▄▃▅▄     │  ← 스파크라인 (축·눈금 없음)
│ ● 정상        │  ← 상태 배지
└──────────────┘
```

### 실험 카드 — 색이 곧 판정

| 판정 | 색 | 조건 |
|---|---|---|
| 성공 | emerald | 주지표 유의 개선 + 가드레일 이상 없음 |
| **주의 필요** | **amber** | **주지표 개선 + 가드레일 악화** |
| 효과 없음 | slate | 유의하지 않음 |
| **무효** | **rose** | **SRM 등 실험 자체가 깨짐** |

**판정 순서가 중요합니다. SRM을 먼저 봅니다.**

> **무효인 실험은 지표를 아예 보여주지 마십시오.**
> 배정이 깨진 실험의 숫자를 화면에 띄우면 사람은 그 숫자를 기억합니다.
> 사유만 적고 값은 계산조차 하지 않는 것이 설계입니다.

### 효과는 신뢰구간으로 보여준다

p값 하나만 보면 **얼마나** 좋아졌는지 모릅니다. 구간을 그려야 크기가 보입니다.

```
        ├────●────┤
   0  +4.62  +7.10  %p
```

0을 지나면 유의하지 않다는 뜻입니다. 축에 0 기준선을 반드시 그립니다.

### 인터랙션 두 가지

**시뮬레이터** — "만약 여기서 멈췄다면?" 슬라이더로 관측 시점을 바꿉니다.
말로 설명하는 대신 **손으로 겪게** 합니다.

**계산기** — 개선폭 슬라이더가 즉시 금액이 됩니다.
제안이 숫자로 환산되는 지점이고, 발표에서 이 화면 하나로 설득이 끝납니다.

## 2-4. 리포트

좌측 목차 · 우측 본문. 8장 중 **3장은 사람이 씁니다.**

| 장 | 작성 |
|---|---|
| 1 요약 · 3 방법 · 4 결과 · 5 실험 · 7 한계 | 자동 |
| **2 배경 · 6 해석 · 8 제안** | **사람** |

> **해석과 제안을 자동화하지 마십시오.** 그것이 사람의 책임이고, 이 수업의 결론입니다.

### 자동 생성 문장은 스스로를 검사한다

관측 데이터로는 인과를 주장할 수 없습니다. 다음 표현이 섞이면 경고를 띄웁니다.

```
때문에 · 덕분에 · 효과로 · 입증되었 · 증명되었 · 확실히
```

### 7장 한계는 검증 결과에서 조립한다

사람이 매번 쓰는 것이 아니라, **경고 항목을 그대로 한계로 옮깁니다.**
검증에서 경고가 났는데 한계에 안 적히면 그 경고는 사라진 것과 같습니다.

## 2-5. 아카이브

실행 이력 · 기간 비교 · **재현**.

**재현이 핵심입니다.** 같은 입력이면 같은 결과가 나와야 하고,
그것을 확인할 수 없으면 자동화를 신뢰할 수 없습니다.

> **계산에 현재 시각을 쓰지 마십시오.** `datetime.now()`가 지표 계산에 끼면
> 어제 돌린 결과를 오늘 재현할 수 없습니다. 시각은 기록에만 씁니다.

---

# 3. 디자인 시스템

## 3-1. 상태 색 — 의미가 고정입니다

| 의미 | 이름 | 값 |
|---|---|---|
| 통과 / 정상 | emerald | `#10b981` |
| 경고 / 표본 부족 / 가드레일 악화 | amber | `#f59e0b` |
| 차단 / 검증 실패 / 실험 무효 | rose | `#f43f5e` |
| 데이터 없음 / 효과 없음 | slate | `#64748b` |

**이 넷은 바꾸지 마십시오.** 색이 판정을 전달하므로, 도메인이 달라도 의미는 같습니다.
브랜드 색(강조·링크·차트 기본)은 자유롭게 바꿔도 됩니다.

## 3-2. 타이포

| 용도 | 크기 | 굵기 |
|---|---|---|
| 화면 제목 | 24px | 800 |
| 섹션 | 18px | 700 |
| 본문 | 14px | 400 |
| **지표 숫자** | **32px** | **700** |
| 캡션 | 12px | 400 |

**숫자에는 `tabular-nums`를 겁니다.**

```css
.num { font-variant-numeric: tabular-nums; font-feature-settings: "tnum"; }
```

자릿수가 흔들리면 싸구려로 보입니다. 이 한 줄이 완성도를 크게 바꿉니다.

## 3-3. 간격

**4px 배수만 씁니다** (4·8·12·16·24·32·48).
카드 내부 여백 24px(조밀한 카드는 16~18px), 카드 사이 16px, 섹션 사이 26px.

## 3-4. 컴포넌트

| 이름 | 용도 |
|---|---|
| `badge(level, text)` | 상태 배지 |
| `kpi_card(label, value, sub, level)` | 지표 카드 |
| `context_bar(run)` | 상단 컨텍스트 |
| `section(title, hint)` | 섹션 제목 |
| `callout(text, kind)` | 강조 문장 |
| `stepper(steps, current)` | 진행 표시 |
| `logbox(entries)` | 실행 로그 |

**색과 간격은 `viz/ui.py`에서만 정의합니다.** 페이지마다 스타일을 흩뿌리면
한 곳을 고쳐도 다른 곳이 안 따라옵니다.

## 3-5. 차트

| 용도 | 도구 | 이유 |
|---|---|---|
| 화면 | **Plotly** | 인터랙션 |
| **PDF** | **matplotlib** | **kaleido 의존을 피한다** |

Plotly를 이미지로 내보내려면 kaleido가 필요한데 환경을 심하게 타서 배포하면 잘 깨집니다.
**화면은 Plotly, 인쇄는 matplotlib** — 이 분리를 지키십시오.

차트 공통 규칙:
- 축·격자는 최소로. 격자는 값 축에만
- 값 레이블을 막대 밖에 직접 적는다 (범례보다 낫다)
- 병목·이상값은 상태 색으로
- 배경 투명, 테두리 없음

---

# 4. 내 도메인으로 바꾸기 ★

**자기 주제로 옮길 때 여기부터 읽으십시오.**

## 4-1. 세 종류로 나눠 생각합니다

| 종류 | 무엇 | 어떻게 |
|---|---|---|
| **고정** | 어느 도메인이든 같은 것 | **그대로 씁니다** |
| **가변** | 도메인마다 다른 것 | `config.py`에서 바꿉니다 |
| **없으면** | 내 데이터에 없는 것 | 아래 4-4 참조 |

## 4-2. 고정 — 바꾸지 마십시오

| 항목 | 왜 |
|---|---|
| **상태 색 4종** | 색이 판정을 전달한다. 의미가 고정이다 |
| **게이트 3종의 성격** | 입구·출구는 되돌릴 수 있고 발송은 못 한다 |
| **검증 → 게이트 순서** | 검증 없이 게이트를 열면 판단할 재료가 없다 |
| **SRM을 먼저 보는 순서** | 배정이 깨졌으면 효과를 계산할 이유가 없다 |
| **해석·제안은 사람이 쓴다** | 자동화하는 순간 책임이 사라진다 |
| **인과 단정 표현 금지** | 관측 데이터로는 인과를 주장할 수 없다 |
| **계산에 현재 시각 금지** | 재현할 수 없게 된다 |
| **화면 4분할** | 성격이 다른 것을 섞지 않는다 |

## 4-3. 가변 — `core/config.py` 한 파일

```python
DATASET  = "project3_growth"        # 내 데이터셋 이름
PERIOD   = ("2025-01-01", "2025-12-31")
TABLES   = [...]                    # 내 테이블 목록

FUNNEL_STEPS  = [...]               # ★ 여기가 가장 중요
FUNNEL_LABELS = {...}

CHANNEL_CAC         = {...}         # 채널별 획득 비용
CONTRIBUTION_MARGIN = 0.30          # 공헌이익률
LTV_MONTHS          = 36            # 평균 유지기간

THRESHOLDS = {...}                  # 지표별 경고·위험 기준
MIN_SAMPLE = 1_000
```

`metrics.py`의 `EXP_STEPS`(실험별로 어느 구간을 보는지)도 함께 갈아끼웁니다.

### 가정값과 실측값을 섞지 마십시오

`config.py`에 들어가는 값은 두 종류입니다. **성격이 다르므로 용도를 갈라 씁니다.**

| | 어디서 오는가 | 무엇에 쓰는가 | 무엇에 쓰지 않는가 |
|---|---|---|---|
| `CHANNEL_CAC` · `CONTRIBUTION_MARGIN` · `LTV_MONTHS` | **가정값.** 업계 벤치마크나 재무팀 합의값 | 손익 환산 · 제안 금액 | **실측이라고 말하지 않는다** |
| `ad_spend` 테이블 | **실측.** 집행된 광고비 | 채널 효율 — ROAS · CPC · 노출/클릭 | **총액을 가입자 수로 나눠 CAC를 만들지 않는다** |

**광고비를 가입자 수로 나눈 값은 CAC가 아닙니다.** 광고비에는 가입으로 이어지지 않은
브랜드·리타게팅 집행이 섞여 있고, 반대로 가입자 중에는 광고를 거치지 않은 사람이 있습니다.
개인 단위로 추적되지 않는 한 그 둘은 이을 수 없습니다.

> 리포트에 손익 숫자를 쓸 때는 **"가정값 기반"임을 문장에 남깁니다.**
> 실측값과 가정값이 한 문단에 섞여 있으면 읽는 사람은 둘 다 실측으로 읽습니다.

### 퍼널 단계 매핑 예시

| 이 앱 (통신) | 커머스 | 채용 | B2B 영업 | 교육 | 사내 시스템 |
|---|---|---|---|---|---|
| 랜딩 방문 | 상품 조회 | 공고 조회 | 리드 유입 | 강의 조회 | 요청 접수 |
| 요금제 조회 | 상세 페이지 | 채용 상세 | 미팅 성사 | 수강 페이지 | 검토 착수 |
| 신청 시작 | 장바구니 | 지원서 작성 | 제안서 발송 | 결제 시작 | 승인 요청 |
| 신청 완료 | 결제 완료 | 지원 완료 | 협상 | 결제 완료 | 승인 |
| 개통 | 배송 완료 | 입사 | 계약 체결 | 첫 수강 | 배포 |
| 첫 결제 | 재구매 | 수습 통과 | 첫 청구 | 완주 | 안정화 |

**단계 수는 도메인이 정합니다.** 6개일 이유가 없습니다. 3개여도, 8개여도 됩니다.

## 4-4. 없으면 — 가장 자주 막히는 지점

> **"내 데이터엔 그게 없는데요"** 가 옮기지 못하는 진짜 이유입니다.
> 없으면 빼거나 대체하면 됩니다. 없다고 못 하는 것이 아닙니다.

| 없는 것 | 대안 | 주의 |
|---|---|---|
| **A/B 실험** | 도입 전후 기간 비교 | **인과를 주장할 수 없습니다.** 7장 한계에 반드시 적습니다 |
| **익명→식별 전환** | `visitor_id = customer_id`로 두면 됩니다 | 처음부터 식별되는 도메인(사내 시스템 등)은 원래 그렇습니다 |
| **비용(CAC)** | 투입 공수(인시)로 환산 | 금액이 아니어도 "무엇을 얼마나 썼는가"면 됩니다 |
| **이탈 개념** | 재이용률 · 반복 요청률 | 계약이 없는 도메인은 "다시 왔는가"로 봅니다 |
| **CS 데이터** | 빼십시오 | 없으면 없는 대로 됩니다 |
| **12개월치** | 있는 만큼 | 기간이 짧으면 계절성 해석을 하지 않습니다 |
| **표본이 적다** | 검정 대신 **기술 통계**만 | 유의성을 억지로 계산하지 마십시오. 4-5 참조 |
| **여러 테이블** | 한 테이블이어도 됩니다 | 조인이 없으면 조인 함정도 없습니다 |

## 4-5. 표본이 적을 때

작은 조직·내부 데이터는 표본이 수십~수백입니다. 이때:

- **p값을 계산하지 마십시오.** 유의하게 나오지 않고, 나와도 못 믿습니다
- 대신 **실제 값과 변화량**을 보여줍니다
- 화면에 `slate` 색으로 **"표본 부족"** 을 명시합니다
- `config.MIN_SAMPLE`을 내 상황에 맞게 낮추되, **경고는 없애지 마십시오**

> 모른다는 것을 아는 것도 결론입니다. 억지로 유의성을 만들어 내는 것이 훨씬 나쁩니다.

## 4-6. 옮기는 순서

```
1. config.py 의 FUNNEL_STEPS 를 내 단계로 바꾼다
2. load.py 가 내 파일을 읽게 한다
3. 앱을 켠다 → 깨진다
4. metrics.py 의 컬럼명을 하나씩 맞춘다
5. validate.py 에서 내 데이터에 맞지 않는 검증을 뺀다
6. 없는 것은 4-4 표대로 대체하거나 지운다
7. THRESHOLDS 를 내 기준으로 정한다  ← 남이 정해줄 수 없다
```

**3번에서 깨지는 것이 정상입니다.** 한 번에 되면 오히려 뭔가 안 바꾼 것입니다.

---

# 5. 만드는 순서

| 순서 | 만드는 것 | 확인 |
|---|---|---|
| 1 | `config.py` + `load.py` | 데이터가 읽히는가 |
| 2 | `app.py` 골격 + `ui.py` | 4개 화면이 이동되는가 |
| 3 | `metrics.py` 퍼널 | 숫자가 손으로 센 것과 맞는가 |
| 4 | 대시보드 지표 카드 + 퍼널 | — |
| 5 | `validate.py` + `gates.py` | 게이트가 되돌려지는가 |
| 6 | 실행 화면 | 끝까지 도는가 |
| 7 | 실험 카드 | 판정 색이 맞는가 |
| 8 | 리포트 + PDF | 한글이 안 깨지는가 |
| 9 | 아카이브 | 재현되는가 |
| 10 | 배포 | URL이 뜨는가 |

**3번을 건너뛰지 마십시오.** 퍼널 숫자를 SQL이나 손계산으로 한 번 검증하지 않으면,
그 뒤 모든 화면이 틀린 숫자 위에 지어집니다.

시뮬레이터·계산기는 **선택 과제**입니다. 나머지가 다 된 뒤에 하십시오.

## 점검은 조작까지

```bash
python tests/test_app.py
```

**HTTP 200은 렌더 성공이 아닙니다.** Streamlit은 웹소켓 연결 시 스크립트를 실행하므로,
페이지가 200을 반환해도 안에서 예외가 났을 수 있습니다.
`AppTest`로 버튼을 누르고 게이트를 통과시키고 PDF를 만들어 봐야 압니다.

---

# 6. 배포

## 6-1. 데이터를 어떻게 실을 것인가

| 방식 | 결과 |
|---|---|
| 스냅샷 (계산 결과만) | **실행 화면과 재현이 껍데기가 됩니다.** 쓰지 마십시오 |
| **원본 parquet 동봉** | **모든 화면이 실제로 동작합니다** |
| DB 직접 연결 | 공개 URL에서는 위험 (아래) |

**parquet으로 동봉하십시오.** CSV 78MB가 parquet 11MB가 됩니다.

> **공개 URL에 데이터 웨어하우스를 직결하지 마십시오.**
> 누구나 페이지를 열어 쿼리를 돌릴 수 있고, 그 비용은 여러분이 냅니다.

## 6-2. 메모리

| | 그냥 읽으면 | dtype 최적화 |
|---|---:|---:|
| 메모리 | 393MB | **129MB** |

Streamlit Community Cloud 무료 한도가 **1,024MB**입니다. 최적화 없이는 위험합니다.

```python
def optimize(df):
    for col in df.columns:
        s = df[col]
        if s.dtype == "object":
            # 카디널리티가 낮은 문자열만 category로. ID는 그대로 둔다
            if len(s) and s.nunique(dropna=True) / len(s) < 0.5:
                df[col] = s.astype("category")
        elif str(s.dtype).startswith("int"):
            df[col] = pd.to_numeric(s, downcast="integer")
        elif str(s.dtype).startswith("float"):
            df[col] = pd.to_numeric(s, downcast="float")
    return df
```

`@st.cache_data`로 캐시하면 적재는 앱 수명 동안 1회입니다.

## 6-3. 절차

```
1. GitHub 저장소 (공개)
2. share.streamlit.io → Sign in with GitHub
3. Create app → Repository / Branch / Main file path = app.py
4. Deploy  (첫 배포 3~5분)
```

## 6-4. 저장소에 올리면 안 되는 것

```gitignore
__pycache__/
*.pyc
runs/*.json
.streamlit/secrets.toml     ← 인증 정보
```

**인증 키를 커밋하지 마십시오.** 한 번 올라가면 커밋을 지워도 히스토리에 남습니다.
필요하면 Streamlit Cloud의 Secrets(웹 UI)에 넣습니다.

## 6-5. 폰트 라이선스

| 폰트 | 재배포 |
|---|---|
| **Noto Sans KR** (OFL) | **가능** |
| 맑은 고딕 | **불가** |

앱에 폰트를 동봉하면 그것은 **재배포**입니다. 로컬에서 잘 나온다고 배포해도 되는 것이 아닙니다.

---

# 7. 흔히 겪는 함정

레퍼런스를 만들면서 실제로 겪은 것들입니다. 여러분도 겪습니다.

## 7-1. category dtype의 부작용

메모리 최적화로 문자열을 `category`로 바꾸면 두 곳에서 터집니다.

```python
# ✕ category에 map을 하면 결과도 category라 평균이 안 된다
df.channel.map(CAC).mean()
      # TypeError: 'Categorical' does not support reduction 'mean'

# ○
df.channel.astype(str).map(CAC).astype(float).mean()
```

```python
# ✕ category를 바로 to_datetime 하면 비교에서 터진다
pd.to_datetime(df.event_date) < ts

# ○ 공용 헬퍼를 하나 두고 전부 그것만 쓴다
def to_dt(s):
    return pd.to_datetime(s.astype(str), errors="coerce")
```

`groupby`에도 `observed=True`를 넣지 않으면 경고가 쏟아집니다.

## 7-2. PDF 한글

- 폰트를 지정하지 않으면 **네모(두부)** 가 나옵니다
- `fpdf2`는 **상태 저장형**입니다. `set_font`·`set_text_color`가 이후 계속 유지되므로 블록마다 명시합니다
- **Noto Sans KR에 그리스 문자가 없습니다.** `α=0.05` 같은 표기는 PDF에서 사라집니다 → "유의수준 0.05"로 씁니다

## 7-3. 테스트

- **HTTP 200은 렌더 성공이 아닙니다**
- `AppTest.from_file("pages/x.py")`로 페이지를 단독 실행하면 `st.page_link`가 터집니다.
  페이지 레지스트리가 없어서 생기는 **테스트 한계**이지 앱 버그가 아닙니다.
  `app.py`에서 시작해 `switch_page()`로 옮겨야 실제 앱과 같은 경로가 됩니다
- `AppTest`의 `session_state`는 `.get()`을 제공하지 않습니다. `"key" in at.session_state`를 씁니다

## 7-4. 그 밖에

- `use_container_width`는 사용 중단 예정입니다 → `width="stretch"`
- 자정 근처 이벤트에 초를 더하면 다음 날짜로 넘어갑니다. 기간 안으로 고정하십시오
- 월 이탈률을 `(1-연간유지율)/12`로 계산하면 복리가 안 맞습니다 → `1 - 유지율**(1/12)`

---

## 부록 — 자기 점검

만들고 나서 확인하십시오.

- [ ] 컨텍스트 바가 4개 화면에 다 있는가
- [ ] 차단이 있을 때 게이트 통과 버튼이 비활성화되는가
- [ ] 게이트 3이 코드에서 되돌림을 거부하는가
- [ ] 판단 근거가 기록에 남는가
- [ ] 무효인 실험의 숫자를 감추는가
- [ ] 신뢰구간에 0 기준선이 있는가
- [ ] 표본이 부족할 때 명시하는가
- [ ] 자동 생성 문장에 인과 단정 표현이 없는가
- [ ] 검증 경고가 리포트 7장 한계로 이어지는가
- [ ] 해석·제안이 비어 있으면 PDF에 "작성되지 않음"이 찍히는가
- [ ] 두 번 돌려서 같은 값이 나오는가
- [ ] 지표 숫자에 `tabular-nums`가 걸려 있는가
- [ ] 배포본 메모리가 한도 안인가
- [ ] `secrets.toml`이 `.gitignore`에 있는가
- [ ] PDF 한글이 안 깨지는가
