# Claude가 낸 숫자를 검증하는 법

**Day2에서 손계산과 값이 안 맞을 때 이 문서를 펴십시오.**

수업 중에 다 읽지 않아도 됩니다. 막혔을 때 1절의 표를 보고, 시간이 나면 나머지를 읽으십시오.

---

# 0. 왜 검증이 필요한가

**Claude는 "이거 틀렸을 수도 있어요"라고 말하지 않습니다.**

코드가 돌고 숫자가 나오면, 그 숫자가 맞는지는 아무도 안 봅니다. 그런데 계산이 틀리는 방식은
대개 **조용합니다** — 에러가 나지 않고, 그럴듯한 값이 나옵니다.

| | 코드가 안 돌 때 | **숫자가 틀렸을 때** |
|---|---|---|
| 티가 나나 | **난다.** 빨간 글씨 | **안 난다** |
| 언제 발견되나 | 즉시 | **한참 뒤에, 또는 영영** |
| 고치기 | 쉽다 | 그 위에 쌓은 걸 다 다시 |

> **그래서 대조가 필요합니다.** 대조하지 않으면 틀린 줄 모르고 넘어갑니다.

## 이건 Claude를 못 믿는다는 말이 아닙니다

사람이 짜도 똑같이 틀립니다. 차이는 하나입니다.

```
사람이 짜면    →  짜는 동안 무엇을 세는지 계속 본다
시켜서 받으면   →  결과만 본다. 무엇을 셌는지는 안 본다
```

**빠른 만큼 확인이 필요합니다.** 이 문서는 그 확인을 빠르게 하는 법입니다.

---

# 1. 자주 틀리는 자리 다섯

**증상을 보고 여기를 찾으십시오.**

| # | 어디서 | 증상 |
|---|---|---|
| 1 | **조인** | 인원이 원본보다 많다 · 평균이 이상하다 |
| 2 | **중복** | 합계가 안 맞는다 · 비율이 100%를 넘는다 |
| 3 | **결측** | 합계가 전체보다 적다 · 특정 구간만 비어 있다 |
| 4 | **타입** | 정렬이 이상하다 · 날짜 비교가 안 된다 |
| 5 | **순서** | 값이 조금씩 다르다 |

---

## 1-1. 조인 — 한 대상이 여러 번 세어집니다

**한 대상에 행이 여럿인 표를 그냥 붙이면 대상이 복제됩니다.**

```
대상 표          한 행 = 대상 1건        1,000행
활동 표          한 행 = 대상 1건 × 월    12,000행

그냥 붙이면  →  대상이 12번씩 복제된다  →  평균이 전부 틀어진다
```

**증상**

| | |
|---|---|
| 인원이 원본 행 수보다 **많다** | 가장 확실한 신호 |
| 평균이 이상하게 낮거나 높다 | 큰 대상이 여러 번 세어졌다 |
| 합계가 몇 배로 뛴다 | |

**확인법**

```
조인하기 전과 후의 행 수를 각각 보여줘.
그리고 대상 ID의 고유 개수도 같이.
```

**행 수가 늘었는데 고유 ID 수가 그대로면 복제된 것입니다.**

**고치는 법** — 붙이기 전에 **대상 단위로 먼저 집계**합니다.

```
활동 표를 대상 ID 기준으로 먼저 합계 내고,
그 결과를 대상 표에 붙여줘. 붙인 뒤 행 수가 그대로인지 확인해줘.
```

---

## 1-2. 중복 — 같은 것이 두 번 들어 있습니다

원본에 중복이 있는 경우와, 계산 과정에서 생기는 경우가 있습니다.

| 어디서 오나 | 예 |
|---|---|
| 원본에 있다 | 시스템 이관 중 같은 기록이 두 번 들어옴 |
| 같은 대상이 여러 번 행동했다 | 한 사람이 세 번 방문 |
| 조인으로 생겼다 | 1-1 참고 |

**두 번째가 중복인지 아닌지는 도메인이 정합니다.**

```
한 사람이 세 번 방문했다
   "방문 수"를 세면      →  3건이 맞다
   "방문한 사람 수"를 세면 →  1명이 맞다
```

**확인법**

```
[ 키 컬럼 ] 기준으로 완전히 같은 행이 몇 개인지 세어줘.
그리고 [ 키 ] 가 같은데 다른 컬럼이 다른 행도 몇 개인지.

둘을 나눠서 보여줘. 지우지는 마.
```

**"지우지는 마"를 넣으십시오.** 중복인지 정상인지는 여러분이 판단합니다.

---

## 1-3. 결측 — 조용히 빠집니다

빈 값이 있으면 계산에서 **그 행이 통째로 빠지는** 경우가 많습니다. 에러는 안 납니다.

**증상**

| | |
|---|---|
| 쪼갠 칸들의 합이 **전체보다 적다** | 어디로 갔나 |
| 특정 구간만 값이 없다 | 그 기간에 수집이 안 됐을 수 있다 |
| 평균이 예상보다 높다 | 낮은 값들이 빠졌을 수 있다 |

**확인법**

```
각 컬럼의 결측 건수와 비율을 보여줘.
그리고 결측인 행들이 어떤 특징을 갖는지도 —
특정 기간이나 특정 값에 몰려 있는지.
```

**마지막 줄이 중요합니다.** 결측이 **무작위로 흩어져 있는 것**과 **한쪽에 몰려 있는 것**은
전혀 다릅니다. 몰려 있으면 그 자체가 정보입니다.

> **채우지 마십시오.** 구조적으로 없는 것을 평균이나 0으로 채우면 데이터를 위조하는 것입니다.
> 8주차 Day1에서 다룬 그것입니다.

---

## 1-4. 타입 — 숫자처럼 보이는데 글자입니다

CSV로 읽으면 흔합니다.

| 증상 | 무슨 일이 |
|---|---|
| 정렬하면 `1, 10, 2, 3` | 숫자가 아니라 글자로 읽혔다 |
| 날짜 비교가 안 된다 | 날짜가 아니라 글자다 |
| 합계가 이상하게 크다 | 글자를 이어붙였다 |
| `1,234` 가 계산이 안 된다 | 쉼표 때문에 숫자로 안 읽힌다 |

**확인법**

```
각 컬럼의 타입을 보여줘.
숫자여야 하는데 글자로 읽힌 게 있으면 알려줘.
```

**고칠 때 주의** — 바꾸다가 값이 사라질 수 있습니다.

```
[ 컬럼 ] 을 숫자로 바꿔줘.
바꾸기 전과 후의 결측 건수를 비교해서 보여줘.
```

**바꾼 뒤 결측이 늘었다면** 숫자로 못 바꾼 값들이 있다는 뜻입니다. 그게 뭔지 봐야 합니다.

---

## 1-5. 순서 — 언제 거르느냐로 값이 달라집니다

```
먼저 거르고 계산한다   vs   계산하고 나서 거른다
```

**같은 결과가 나올 것 같지만 다릅니다.** 특히 평균·비율에서 그렇습니다.

```
전체 100건 중 40건을 거른 뒤 평균     →  60건의 평균
전체 평균을 낸 뒤 40건을 뺀다         →  이건 다른 값이다
```

**증상**: 값이 **조금씩** 다릅니다. 자릿수는 맞는데 소수점이 안 맞습니다.

**확인법**

```
이 계산에서 필터가 몇 번 걸리고 각각 어느 시점인지 알려줘.
그리고 필터 전후의 행 수도.
```

---

# 2. 시키기 전에 줘야 할 것

**안 주면 Claude가 추측합니다.** 추측이 맞을 때도 있지만 확인할 방법이 없습니다.

| 줘야 할 것 | 안 주면 |
|---|---|
| **한 행이 무엇인가** (그레인) | 아무거나 셉니다 |
| **무엇을 하나로 셀 것인가** | 행을 그대로 셉니다 |
| **기간** | 전체를 봅니다. 최근 것이 덜 익었어도 |
| **컬럼의 의미** | 이름으로 추측합니다 |
| **무엇을 빼야 하는가** | 안 뺍니다 |

## 짧게 주는 법

매번 길게 쓸 필요는 없습니다. **처음 한 번만 제대로** 주면 됩니다.

```
이 데이터를 설명한다.

  [ 파일명 ] — 한 행이 [ 무엇 ] 이다. [ N ] 행.
     [ 컬럼 ] 은 [ 의미 ] 다
     [ 컬럼 ] 은 [ 의미 ] 다.  ★ [ 주의할 점 ]

  기간은 [ 시작 ~ 끝 ] 이고, [ 시점 ] 이후는 아직 덜 쌓였다.

앞으로 이 데이터로 계산할 때 위를 전제로 해줘.
```

**★ 표시로 주의점을 넣으십시오.** 예를 들면 이런 것들입니다.

| 주의점 예 | 왜 |
|---|---|
| *"ID 순서가 시간 순서가 아니다"* | 정렬을 ID로 하면 틀립니다 |
| *"이 컬럼은 완료된 건에만 값이 있다"* | 결측이 정상입니다 |
| *"이 표는 한 대상에 여러 행이 있다"* | 조인 사고를 막습니다 |

> **한 번 준 것도 대화가 길어지면 잊힙니다.** 그래서 `CLAUDE.md` 에 적어 두는 것이
> 좋습니다. → 「Claude와 글을 쓸 때」 5절

---

# 3. 검증을 시키는 법

**세 가지 방법이 있습니다.** 하나만 해도 대부분 잡힙니다.

## 3-1. 중간 산출물을 요구합니다

**결과만 받지 말고 과정을 보여달라고 하십시오.**

```
계산하기 전에 먼저 보여줘.

- 원본 행 수
- 필터를 걸고 난 뒤 행 수
- 고유 대상 수

그 다음에 계산해줘.
```

**행 수가 어디서 얼마나 줄었는지 보이면** 이상한 곳이 눈에 띕니다.

## 3-2. 같은 것을 두 방법으로 시킵니다

```
같은 값을 다른 방법으로 한 번 더 계산해줘.
두 결과가 같은지 비교해서 보여줘.
```

**다르게 나오면 둘 중 하나가 틀린 것입니다.** 어느 쪽인지는 그때 찾으면 됩니다.

> 이게 **손계산 대조를 대신하지는 않습니다.** 두 방법 다 같은 오해를 하고 있으면
> 둘 다 틀린 채로 일치합니다. 그래도 안 하는 것보다 낫습니다.

## 3-3. 반대로 세어봅니다

```
[ 조건 ] 에 해당하는 건수를 셌다.
이번엔 해당하지 않는 건수를 세어줘.
둘의 합이 전체와 맞는지 확인해줘.
```

**합이 안 맞으면 어느 쪽에도 안 들어간 것이 있다는 뜻입니다.** 대개 결측입니다.

## 어느 것을 언제 쓰나

| 상황 | 방법 |
|---|---|
| 처음 계산할 때 | 3-1 중간 산출물 |
| 값이 미심쩍을 때 | 3-2 두 방법 |
| 쪼갠 합이 안 맞을 때 | 3-3 반대로 세기 |

---

# 4. 한 번에 하나씩

**여러 개를 한 프롬프트에 넣으면 뭉갭니다.**

```
✕   퍼널 계산하고, 분해도 하고, 지표 카드도 만들고, 색도 넣어줘

○   퍼널을 계산해줘. 결과를 보고 다음을 정하겠다
```

## 왜 그런가

| 한 번에 여럿 시키면 | |
|---|---|
| 중간에 틀려도 그냥 진행한다 | 어디서 틀렸는지 못 찾는다 |
| 앞 결과를 확인 안 하고 뒤를 만든다 | 틀린 것 위에 쌓인다 |
| 프롬프트가 길어져 일부를 놓친다 | |

## 다만 이런 건 같이 시켜도 됩니다

| 같이 시켜도 되는 것 | 예 |
|---|---|
| 만들고 **바로 확인**하는 것 | "계산하고 결과를 표로 보여줘" |
| 한 파일 안의 **관련된 것** | "이 함수와 그게 쓰는 헬퍼를 같이" |
| **되돌리기 쉬운** 것 | "주석을 정리해줘" |

**기준은 "틀렸을 때 어디서 틀렸는지 알 수 있는가"입니다.**

---

# 5. 에러가 났을 때

## 5-1. 요약하지 말고 그대로 붙여넣으십시오

```
✕   에러가 났어요. 뭔가 컬럼이 없다는 것 같은데요

○   (에러 메시지 전체를 그대로 붙여넣기)
```

**에러 메시지에는 여러분이 안 읽는 정보가 들어 있습니다** — 어느 파일 몇 번째 줄인지,
어떤 값이었는지. 요약하면 그게 사라집니다.

**길어도 됩니다.** 길수록 좋습니다.

## 5-2. "고쳐줘" 대신 "원인부터"

```
✕   에러 났어. 고쳐줘

○   이 에러가 났다. (에러 전문)
    고치기 전에 원인부터 말해줘.
```

**바로 고치면 무엇이 문제였는지 모른 채 넘어갑니다.** 그러면 같은 것이 또 납니다.

그리고 **원인을 들으면 여러분이 판단할 게 생깁니다** — 정말 그게 원인인지,
고치는 방향이 맞는지.

## 5-3. 같은 에러가 두 번 나면

**고치는 방식이 잘못된 것입니다.** 방향을 바꾸십시오.

```
같은 에러가 또 났다. 아까 고친 방식이 안 맞는 것 같다.
다른 접근이 있는지, 아니면 더 근본적인 문제가 있는지 봐줘.
```

---

# 6. 되돌릴 수 있게 해두기

**원본을 안 건드리게 하는 것이 첫째입니다.**

```
원본 파일은 읽기만 하고 고치지 마.
가공한 결과가 필요하면 별도 파일로 만들어줘.
```

## 큰 것을 바꾸기 전에

```
지금 상태를 백업해두고 시작해줘.
```

또는 git을 쓴다면

```
지금까지 한 것을 커밋해줘. 메시지는 [ 무엇을 했는지 ] 로.
```

**"되돌리고 싶다"가 생기는 순간은 반드시 옵니다.** 그때 되돌릴 게 있어야 합니다.

## 이미 망가뜨렸다면

```
방금 바꾼 것을 되돌려줘. 무엇을 바꿨는지 먼저 알려주고.
```

**"무엇을 바꿨는지 먼저"** 를 넣으십시오. 되돌리는 것도 확인하고 해야 합니다.

---

# 7. 체크리스트

계산을 시키고 나서 이것만 확인하십시오. **1분이면 됩니다.**

```
□  행 수가 원본과 말이 되는가 (늘었으면 조인, 줄었으면 필터·결측)
□  쪼갠 칸들의 합이 전체와 맞는가
□  자릿수가 예상 범위인가 (3%인가 30%인가)
□  두 번 돌려서 같은 값이 나오는가
□  손으로 센 것이나 표본 100건과 자릿수가 맞는가
```

## 안 맞을 때 의심하는 순서

```
1.  그레인      무엇을 하나로 셌는가        ← 대부분 여기
2.  분모        전체를 무엇으로 잡았는가
3.  필터        무엇을 뺐는가 · 언제 뺐는가
4.  기간        언제부터 언제까지인가
5.  타입        숫자가 글자로 읽히지 않았는가
```

**순서대로 보십시오.** 5번부터 뒤지면 오래 걸립니다.

## 그리고 찾으면 기록하십시오

```
처음에 [ 무엇 ] 이 틀렸고, 원인은 [ 무엇 ] 이었다.
```

**이 한 줄이 다음에 같은 것을 빨리 찾게 해줍니다.** 그리고 발표에서 답할 것이 됩니다.

---

# 부록. 이 문서를 Claude에게 주는 법

이 문서 자체를 참조하게 할 수 있습니다.

```
숫자를_검증하는_법.md 을 읽고, 지금 만든 계산에서
1절의 다섯 가지 중 걸리는 게 있는지 점검해줘.
```

**다만 스스로 점검한 것을 그대로 믿지는 마십시오.** 결과를 보고 여러분이 판단합니다.
자기가 만든 것을 자기가 검사하면 같은 오해를 반복할 수 있습니다.

**가장 확실한 것은 여전히 손으로 세어보는 것입니다.** 표본 100건이면 충분합니다.
