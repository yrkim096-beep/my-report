# 판단 기준

앱을 만들며 오늘 실제로 내린 판단을 날짜별로 쌓는다. 도메인 이름이 남아 있으면 그건 예시이지 기준이 아니다 — 빼거나 일반화한다.

---

## 2026-09-01 — Day1: 앱이 데이터를 거부할 수 있는가

**배운 것**

- 차단과 경고를 가르는 것은 "계산이 틀리는가"다. 틀린다 → 차단. 해석만 조심하면 된다 → 경고.
- 검증 규칙은 적게 만든다. 차단을 늘리면 아무것도 못 돌리고 결국 검증을 끄게 된다.
- 임계값에는 근거를 적는다. 근거 없는 임계값은 남의 것이다.
- 메시지에는 숫자를 넣는다. "실패"는 판단 재료가 아니다.
- 결측은 오류일 수도 구조일 수도 있다. 구조면 채우지 않는다. 채우면 위조다.
- 검증은 만든 것으로 끝나지 않는다. 깨뜨려 넣어 실제로 막히는지 본다. 경고만 띄우고 진행되는 검증은 없는 것과 같다.
- 캐시는 코드가 바뀌면 다시 읽지만, 같은 이름의 데이터 파일 내용만 바뀐 건 스스로 알아채지 못한다. 파일을 바꿔 넣었는데 결과가 그대로면 먼저 캐시(또는 프로세스 재시작)를 의심한다.
- 화면에 보인다고 기록이 저장됐다는 뜻은 아니다. 세션에만 있는 상태와 파일로 저장된 상태는 다르다 — 다음 단계까지 가야 저장되는 구조라면, 지금 상태가 어디까지 저장되는지 먼저 확인한다.
- 뒤 단계(계산)가 아직 안 맞아도 앞 단계(게이트)의 판단은 그 자체로 유효하다. 하나가 깨졌다고 이미 통과시킨 판단까지 무효가 되는 건 아니다.

**오늘 내린 결정**

- **차단으로 정한 것 — 여러 테이블을 잇는 연결키(공통 ID)가 한 테이블에서라도 없으면 차단.** 연결키가 없으면 테이블을 이어 붙일 방법이 없어 그 뒤의 모든 계산이 불가능해지기 때문이다(계산이 확실히 틀리는 경우).
- **경고로 정한 것 — 최상위(가장 앞 단계) 테이블의 행 수가 미리 정해둔 최소표본에 못 미치면 경고.** 계산 자체는 되지만 세그먼트 단위 결과는 못 믿기 때문이다. 다만 0행(완전히 빔)은 계산 자체가 불가능하므로 경고가 아니라 차단으로 따로 갈랐다.
- **경고로 정한 것 — 핵심 이벤트 날짜(첫 유입 시점)가 관측 기간을 벗어나면 경고.** 그 행만 빼고 계산하면 되므로 전체를 막을 이유는 없다고 판단했다. 다만 원래 미래로 자연히 뻗어나가는 파생 날짜(종료일·정산 주기 등)는 이 검사에서 제외했다 — 정상적으로 기간 밖으로 나가는 값까지 걸리면 검증이 항상 울려서 신호로서 의미가 없어진다.

---

## 2026-09-02 — Day2: 유지 퍼널과 이탈을 정의하기

**배운 것**

- 서로 배타적인 상태값 하나로 표현되는 컬럼(A거나 B거나 C인 것)은 그 자체로는 퍼널 단계가 아니다. 앞 단계를 순서대로 거치며 누적되는 게 아니라 종료 시점에 라벨 하나로 갈리는 분류일 뿐이다 — 퍼널이 아니면 퍼널이라 부르면 안 된다.
- 누적 발생 횟수로 정의한 단계(예: N번째 이상 반복된 이벤트가 있었는가)는 진짜 퍼널이 될 수 있다. N번째 조건을 만족하면 N-1번째도 자동으로 만족하는 중첩 구조이기 때문이다. 다만 이때 그레인은 "대상 1건"이 아니라 "대상×관측 기간"이 된다 — 늦게 들어온 대상은 그 기간을 채울 시간이 아직 없었을 뿐인데 미달로 잘못 잡히기 쉽다.
- 앞 단계 없이 다음 단계에 나타나는 대상이 있는지 실제로 세어봐야 한다. 거의 없으면(전체의 1~2% 수준) 그 구조를 퍼널로 써도 되고, 많으면 그건 퍼널이 아니라는 신호다.
- "마지막 활동 후 며칠 지났는가"로 이탈을 판정할 때, 기준 시점(anchor)을 데이터에 남은 최신 값으로 잡으면 안 된다 — 우연히 늦게까지 관측된 표본 한둘 때문에 꼬리가 흔들려 전체 판정이 왜곡된다. 프로젝트가 미리 선언해 둔 고정된 관측 종료 시점을 쓰는 편이 안전하다.
- 더 나아가서, 결과가 데이터에 이미 명시적으로 기록돼 있다면(예: 종료 상태값) 그걸 그대로 쓰는 게 "마지막 활동 후 며칠" 같은 파생 지표보다 낫다. 파생 지표는 기준 시점을 무엇으로 잡을지의 문제를 항상 안고 간다.
- 다만 아직 결과가 정해지지 않은 대상(현재 진행 중)은 유지도 이탈도 아니다 — 판정 대상에서 빼야지 억지로 어느 한쪽에 넣으면 안 된다. 대신 "위험 신호" 같은 별도 표시로 남겨서 정보를 버리지 않는다.
- 최근 묶음(코호트)의 수치가 낮아 보일 때 "성과 문제인지" "아직 결과가 나올 시간이 안 지난 것뿐인지"는, 그 코호트가 실제로 얼마나 관측됐는지를 다음 단계까지 걸리는 일반적인 소요 시간과 비교해서 가른다 — 관측된 시간이 소요 시간보다 충분히 길면 시간 부족은 핑계가 안 된다.
- 캐시되는 계산 함수 안에는 현재 시각을 넣지 않는다. 화면에 "지금 몇 시에 그려졌는지"를 보여주는 용도로만 시각을 쓰고, 판정·집계 로직과는 분리한다.

**오늘 내린 결정**

- **그레인을 "대상 1건"으로 정했다.** 유지·이탈을 다루는 퍼널에서 대상은 한 번 맺으면 끝나는 단위이고, 그 아래 반복되는 하위 이벤트(예: 주기적으로 발생하는 활동 기록)는 집합으로 묶어 "그 대상이 이 단계에 도달했는가"만 센다. 하위 이벤트 건수를 그대로 세면 반복이 많은 대상이 과대 대표되기 때문이다.
- **유지·이탈은 활동의 공백 기간이 아니라, 데이터에 이미 있는 종료 상태값으로 정의했다.** "종료 후 관계를 다시 이어갔다"에 해당하는 상태값은 유지로, "종료되고 안 이어갔다"에 해당하는 상태값들은 이탈로 놓았다. 파생 지표(마지막 활동 후 며칠)보다 기준 시점 문제 없이 더 명확하게 판정할 수 있다는 게 근거다. 아직 종료되지 않은 대상은 유지·이탈 어느 쪽도 아니므로 분모에서 뺐다.

**Claude가 말하지 않고 정한 것 중 잡아낸 것** ★

- 이탈을 "마지막 활동 후 180일 무활동"으로 계산할 때, "기간이 원래 끝나서 활동이 자연스럽게 멈춘 경우"와 "기간이 안 끝났는데 활동만 끊긴 경우"를 구분하지 않고 하나의 숫자로 합쳐서 냈다. 이 구분을 하겠다는 말을 먼저 하지 않았다 — "기간이 끝나면 당연히 이탈 아니냐"는 되물음으로 드러났다. 실제로 그 숫자 중 다수는 그냥 기간이 끝난 것이었고, 진짜 위험 신호는 일부뿐이었다.
- 표에 색이 있는 진행바(값의 비율을 0~1로 표시하는 컬럼)를 요청받은 형식 그대로 만들면서, 그 형식대로면 실제 비율(예: 63.7%)이 화면에는 1/100로 줄어든 값(0.6%)으로 찍힌다는 걸 구현하다가 알았다. 시킨 그대로 만들되, 이 부작용은 먼저 묻지 않고 그냥 넘어갈 뻔한 지점이라 결과를 보여주면서 같이 짚었다.

---

## 2026-09-07 — Day3: 무엇을 보여주지 않을 것인가

**배운 것**

- 조건은 여러 개일 수 있어도 분기는 하나다. 못 믿을 조건에 걸리면 사유를 적고 계산을 멈춘다. 조건이 하나뿐이어도 된다 — 억지로 여러 개를 채우지 않는다.
- 계산해 놓고 화면에 안 그리는 것과, 애초에 계산하지 않는 것은 다르다. 후자여야 한다 — 값이 변수에 있으면 다른 곳(보고서·로그)으로 새어 나갈 수 있다.
- 표본이 적은 항목을 가릴 기준은 건수 구간마다 다르게 잡는다. 하나의 임계값으로 보임/안보임만 가르기보다, 구간별로 "무엇까지는 보여줘도 되는가"를 나눠서 정하는 게 더 정직하다.
- 최소 표본 기준은 실제 항목들의 크기 분포를 보고 고른다 — 이론값이 아니라 관측된 범위의 하한 근처에서 정하면 근거가 생긴다.
- 같은 성격의 최소 표본 기준은 여러 곳에서 재사용한다. 장소마다 다른 숫자를 쓰면 어느 게 맞는 기준인지 알 수 없게 된다.
- 판정 순서는 못 믿을 조건 → 주지표 → 부작용 지표(가드레일) 순으로 고정한다. 주지표가 안 움직였으면 부작용 지표는 아예 보지 않는다 — 부작용을 먼저 보고 주지표로 판정을 뒤집는 순서가 아니다.
- 비교 대상 집단이 없어도(무작위 배정된 실험이 없어도) 전후 비교로 대신할 수 있다. 다만 "인과를 주장할 수 없다"는 문장을 각주가 아니라 카드 본문에 박아 둔다.
- 이동 기준(얼마나 움직여야 의미 있는가)은 과거 데이터로 같은 비교를 여러 시점에 반복해 보고, 그 자연스러운 흔들림보다 확실히 크게 잡는다. 예시로 본 숫자를 그대로 베끼지 않는다.
- 색은 판정을 전달하는 용도로만 쓴다. "못 믿는 것"과 "믿을 수는 있지만 나쁜 값"은 다른 색을 써야 한다 — 같은 색을 쓰면 왜 못 믿는지와 왜 나쁜지가 뒤섞인다.
- 판정 과정은 접힌 상태로 두고, 펼쳐야 어디서 갈렸는지 보이게 한다. 못 믿을 조건에 걸린 경우 그 안에도 실제 값 대신 "계산하지 않음"이라고만 적는다.
- 감춘 값의 근거를 보여줄 땐 조건 값(표본 수, 기준치)만 보여준다. 실제 지표 값·증감·유의확률은 넣지 않는다 — 넣으면 감춘 의미가 없어진다.
- 화면의 필터 상태를 URL에 실으면 링크 하나로 같은 화면을 재현할 수 있다. 다만 그 필터 위젯은 부분 재실행 조각(fragment) 밖에 둬야 한다 — 안에 두면 조각만 다시 그려져 URL과 화면이 어긋난다.
- 이상한 값이 URL로 들어와도 기본값으로 떨어지게 하고, 에러를 내면 안 된다.

**오늘 내린 결정**

- 쪼개는 축은 손을 쓸 수 있는 것 중에서 골랐다. 후보 중 격차가 뚜렷한 두 개는 남기고, 격차가 2%p대에 그친 후보 하나는 쪼개도 갈리지 않아 뺐다.
- 항목 하나(분해 한 칸)의 최소 표본은 실제 항목 크기 범위(수십~수백 건)를 보고, 그 범위의 하한 근처인 50건으로 잡았다 — 최상위 테이블 전체에 쓰는 최소 표본과는 별도로 뒀다. 항목 단위가 훨씬 작아서 같은 숫자를 쓰면 전부 가려지기 때문이다.
- 못 믿을 조건은 "표본 미달" 하나로 정했다. 배정 공정성·최소 기간 조건은 비교할 실험 자체가 없어 해당 없음으로 뒀다 — 조건을 억지로 셋 채우지 않았다.
- 전후 비교의 주지표 이동 기준과 부작용 지표 악화 기준은 과거 실제 비교를 여러 시점으로 반복해 본 뒤, 그 흔들림 폭보다 뚜렷이 크게 잡았다.

---

## 2026-09-07 — Day4: 보고서가 책임을 지도록 만들기

**배운 것**

- 문장을 자동으로 쓸지 사람이 쓸지 가르는 질문은 하나다 — "이 문장이 틀렸을 때 누가 책임지는가?" 사람이 진다면 사람이 쓰고, 사실이 틀린 것뿐이라면 자동으로 써도 된다. 해석과 제안을 자동화하는 순간 책임질 사람이 사라진다.
- 인과 단정 표현을 막는 검사는 자동 생성 문장뿐 아니라 사람이 쓴 문장에도 똑같이 걸어야 한다. 사람이 오히려 그런 표현을 더 자주 쓴다 — 자동 생성분만 검사하면 절반만 검사한 것이다.
- 감춘 값(표본 미달 등으로 화면에서 가린 수치)은 그 값을 가린 칸(표·차트)만 조심해서는 안 지켜진다. 같은 값이 사람이 나중에 쓰는 해석·설명 문단에 다시 등장할 수 있다 — 가린 근거(표본 수·기준치)는 보여주되 실제 수치는 어디에도 새어 나오지 않았는지 문서 전체를 대조해야 한다.
- 감춘 값이 하나 발견되면, 그 문장을 표본이 충분한 부분집합만으로 다시 계산해서 대체해야 한다. 그냥 문장을 얼버무리거나 숫자를 빼기만 하면 남은 주장(예: "이게 1등이다")의 근거 자체가 사라진 채로 남는다.
- 여러 개의 입력 칸을 하나의 제출 단위(폼)로 묶으면, 제출 버튼을 누르기 전까지 화면이 다시 그려지지 않는다. 긴 글을 쓰는 동안 매 글자마다 화면 전체가 재계산되는 문제는 입력 자체가 아니라 "언제 반영할지"의 문제였다.
- 표 형태의 자료를 사람이 직접 고치게 할 때는, 자동으로 조립되는 행(규칙에서 기계적으로 뽑히는 것)과 사람이 스스로 찾아 더해야 하는 행(자동화가 원래 못 찾는 것 — "찾아봤는데 없었다"도 결과다)을 구분해서 보여준다.
- 자동으로 조립된 항목을 사람이 표에서 지울 수 있게 허용하더라도, 지웠다는 사실 자체는 화면에 남겨야 한다. 조용히 사라지면 "그 경고는 원래 없었다"와 "경고가 있었는데 사람이 뺐다"를 구분할 수 없게 된다.
- 몇 초 걸리는 작업은 진행 상태를 단계별로 보여준다. 화면이 멈춘 것처럼 보이면 사람은 버튼을 다시 누르고, 그러면 같은 작업이 중복 실행된다.
- 산출 파일 이름에 만들어진 시각을 넣으면, 같은 종류의 결과물을 여러 번 만들어도 어느 시점 것인지 뒤섞이지 않는다.
- 사람이 판단하는 지점(게이트)을 통과시킨 기록은 실행마다 남기고, 한 화면에서 여러 번의 실행을 나란히 비교할 수 있게 해야 한다 — 그래야 "이번 실행에서 검토를 건너뛰지 않았는가"를 나중에 확인할 수 있다.
- (자동화 테스트를 짜며 확인한 것) 화면에 보이는 안내 문구가 항상 입력칸의 placeholder 속성인 것은 아니다 — 위젯의 라벨로 렌더링되는 경우도 있다. 이 둘을 구분하지 않으면 엉뚱한 이유로 자동화가 막힌다.

**오늘 내린 결정**

- 사람이 쓰는 장 여러 개를 하나의 폼으로 묶기로 정했다. 이유: 한 칸씩 따로 저장하게 하면 긴 문장을 쓰는 도중 계속 화면이 다시 그려져 불편했다.
- 한계(찾아낸 문제) 절의 출처를 세 종류(검증에서 난 경고 · 표본 부족 등으로 판정을 못 한 것 · 찾아봤지만 없었던 것)로 나누고, 앞의 두 종류만 자동으로 채워 넣고 세 번째는 사람이 직접 추가하게 정했다. 이유: 자동화는 규칙이 잡아내는 것만 조립할 수 있고, "찾아봤는데 없었다"는 사람만 판단할 수 있다.
- 만드는 데 시간이 걸리는 산출물은 재실행을 막는 대신 진행 상태를 보여주는 쪽을 택했다. 이유: 멈춘 화면을 오작동으로 오해해서 버튼을 반복해서 누르는 쪽이 더 흔한 실패였다.

---

## 종합 — 화~금 4일 정리 (중복 제거)

날짜별 기록은 위에 그대로 둔다. 여기서는 같은 말이 4일에 걸쳐 반복된 것만 하나로 합친다.

- **못 믿는 것과 나쁜 것은 다르게 다룬다.** 표본이 모자라 판정 자체를 못 하는 경우(못 믿음)와, 판정은 되지만 결과가 나쁜 경우(나쁨)를 같은 색·같은 방식으로 표시하면 안 된다. 못 믿을 조건에 걸리면 그 자리에서 멈추고 사유(표본 수·기준치)만 남기며, 실제 값은 계산 자체를 하지 않는다 — 계산해 놓고 화면에서만 가리는 것과는 다르다. *(Day3 "배운 것"·"오늘 내린 결정")*
- **감춘 값은 표·차트에서 가리는 것만으로 끝나지 않는다.** 그 값의 실제 수치는 사람이 나중에 쓰는 해석·설명 문단에도 다시 등장할 수 있으므로, 문서 전체를 대조해 어디에도 새어 나오지 않았는지 확인해야 한다. 발견되면 표본이 충분한 부분집합으로 다시 계산해 대체한다. *(Day3 "감춘 값의 근거를 보여줄 땐…" + Day4 "감춘 값은 표·차트만 조심해서는…")*
- **화면에 보인다고 저장된 것은 아니다.** 세션에만 있는 상태(예: 아직 제출 전 입력값)와 파일로 저장되는 상태(예: 사람이 판단한 지점을 통과시킨 기록)를 구분해야 한다. 후자는 실행마다 남기고, 여러 실행을 한 화면에서 나란히 비교할 수 있어야 나중에 "이번엔 검토를 건너뛰지 않았는가"를 확인할 수 있다. *(Day1 "화면에 보인다고…" + Day4 "게이트를 통과시킨 기록은…")*
- **임계값·최소 표본 같은 숫자에는 반드시 근거를 적는다.** 근거는 이론값이 아니라 실제 항목들의 크기 분포·과거 시점 간 흔들림 폭처럼 관측된 것에서 고른다. 같은 성격의 기준은 여러 곳에서 재사용한다 — 장소마다 다른 숫자를 쓰면 어느 게 맞는 기준인지 알 수 없다. *(Day1 "임계값에는…" + Day3 "최소 표본 기준은…" · "같은 성격의…" · "이동 기준은…")*
- **검증·검사는 만들었다고 끝이 아니라 실제로 걸리는지 깨뜨려서 확인한다.** 자동 생성 문장이든 사람이 쓴 문장이든 같은 검사를 통과해야 한다 — 사람이 오히려 문제 표현을 더 자주 쓴다. *(Day1 "검증은 만든 것으로…" + Day4 "인과 단정 표현을 막는 검사는…")*
- **판정 기준 시점(anchor)은 데이터에 남은 최신 값이 아니라 프로젝트가 미리 정한 고정 시점을 쓴다.** 표본 하나가 우연히 늦게까지 관측됐다는 이유로 전체 판정이 흔들리면 안 된다. *(Day2 "기준 시점을 최신 값으로…")*
- **판정 순서는 못 믿을 조건 → 주지표 → 부작용 지표로 고정한다.** 주지표가 안 움직였으면 부작용 지표는 아예 보지 않는다. *(Day3에서만 나왔지만 반복 강조돼 종합에 남긴다)*
- **계산 로직 안에는 현재 시각을 넣지 않는다.** 화면에 "언제 계산됐는지"를 보여주는 표시와, 판정·집계 자체에 쓰는 시간 기준은 분리한다. *(Day2 "캐시되는 계산 함수 안에는…")*

---

## 2026-09-07 — 9주차 Day1: 이 분석을 근거로 쓸 수 있는가

**배운 것**

- 맞는 숫자와 근거가 되는 숫자는 다르다. 정확한 값이어도 분모가 없으면 아무도 그것으로 결정하지 못한다.
- 근거에는 넷이 있어야 한다 — 분자·분모, 비교 대상, 비중, 표본(한 건이 바뀌면 얼마나 흔들리는가). 넷 중 하나라도 빠지면 "그게 큰일인지" 판단할 수 없다.
- 근거는 그 자리에서 다시 조회한 것이어야 한다. 요약본(리포트)에서 옮겨 적은 숫자를 쓰지 않는다 — 요약을 요약하면 어디서 틀렸는지 되짚을 수 없고, 그 사이 데이터가 늘었을 수도 있다.
- 같은 비율이라도 분모가 무엇이냐에 따라 다른 값이 된다(전체 대비인지, 그 단계에 도달한 것 대비인지). 어느 분모를 썼는지 항상 적는다.
- 표본을 흔드는 검증은 "한 건이 바뀌면 값이 얼마나 움직이는가"를 실제로 계산해 격차와 비교한다. 흔들림이 격차보다 크면 그 축으로는 결론을 낼 수 없다 — 격차가 진짜인지 우연인지는 계산해야 알지 눈으로 봐서는 모른다.
- 이미 감춘 값(못 믿을 조건에 걸려 화면에서 가려진 것)은 그 상태 그대로 둔다. 그 값을 다른 문서에 다시 꺼내 쓰면 감춘 행위 자체가 무의미해진다.
- 최근 구간의 값이 낮게 나오는 것은 흔히 실제 악화가 아니라 관측 시간이 아직 덜 찬 것이다 — 그 구간에 들어온 대상이 결과가 나오기까지 걸리는 일반적인 시간을 아직 다 채우지 못했을 뿐인 경우가 많다. 이걸 확인하지 않고 "최근에 나빠졌다"를 발견으로 적으면 근거 없는 결론이 된다.
- 전환율이 가장 낮은 구간(비율 기준)과 이탈 인원이 가장 많은 구간(건수 기준)은 서로 다를 수 있다. 다르면 구멍이 두 개라는 뜻이고, 하나만 보고 넘어가면 다른 하나를 놓친다.
- 발견은 숫자 나열이 아니라 문장이다 — 무엇이 얼마(분자/분모)이고, 무엇과 비교했을 때 얼마나 차이 나며, 그것이 전체의 얼마를 차지하는지 세 줄이 다 있어야 발견이라 부를 수 있다.
- 발견에 원인을 섞지 않는다. "무엇 때문에 낮다" 같은 표현이 들어가면, 그건 아직 데이터로 좁히지 않은 추측이고 나중에 그 추측을 정당화하려는 방향으로 분석이 편향된다.
- 표본이 부족해 판정을 보류한 항목은 버리지 않는다. "무엇이 채워지면 판정할 수 있는가"를 적어 두면 그것이 다음에 "다시 볼 것" 후보가 된다.

**오늘 내린 결정**

- 발견의 크기는 격차 × 비중(그 구간 전체 대비 해당 세그먼트가 차지하는 몫)으로 재기로 했다. 절대 건수(연간 환산)까지 같이 냈다 — 비율만으로는 그 격차를 없앴을 때 실제로 몇 건이 달라지는지 알 수 없기 때문이다.
- 두 축(담당자·유입채널) 모두 흔들림 폭보다 격차가 커서 "믿을 수 있는 격차"로 채택했다. 표본 미달로 판정을 보류한 칸(담당자 4곳, 채널 1곳)은 비교에서 뺐지만 목록에는 남겨 "판정 보류" 상태로 표시했다.
- 최근 3개월 월별 전환율은 관측 기간 미달(계약까지 걸리는 중앙값보다 관측 기간이 짧음)로 판단해 발견에서 제외하고 "못 쓰는 숫자"로만 남겼다 — 표본 자체는 있지만 비교 대상으로 쓰기엔 이르다고 봤다.
- 실험(A/B) 기반 비교 항목은 이 데이터에 해당 테이블 자체가 없어 "비교가 깨진 것"이 아니라 "처음부터 해당 없음"으로 구분해 적었다.

---

## 2026-09-14 — 9주차 Day2: 이 제안을 앱에서 바로 볼 수 있는가

**배운 것**

- 리포트(무슨 일이 있었는가)와 제안서(그래서 무엇을 하자는가)는 다른 문서다. 같은 자동/사람 분리 원칙이라도 서로 다른 파일에 따로 적용해야 한다 — 한 파일에 우겨넣으면 두 문서의 목적이 섞인다.
- 절을 자동으로 채울지 사람이 쓸지 가르는 기준은 하나다 — "데이터를 다시 조회하면 똑같이 나오는가?" 그렇다면 자동, 판단이 들어가면 사람이다.
- 후보(예: 다음에 무엇을 적용할지)는 자동으로 나열할 수 있어도, 그중 무엇을 고를지는 사람의 몫이다. 후보 목록 자체를 고칠 수 없게 만들어야 이 경계가 코드로도 지켜진다.
- 문서는 카드(사실이 적힌 재료)를 옮기는 것이지 새로 짓는 것이 아니다. 카드에 없는 값을 화면에 채워 넣으면 그럴듯해 보이지만 아직 안 정한 것을 정한 것처럼 만든다 — 없으면 "미확인"이라고 그대로 띄운다.
- 문장 검사(인과 단정 표현 차단)는 한 번 만든 걸 다른 문서에서도 그대로 재사용한다. 화면마다 새로 만들면 어느 화면은 빠뜨리게 된다.
- 지금 당장 자리(메뉴 위치)가 없어도 기능부터 만들 수 있다. 임시 탭에 붙여 두고, 자리는 다음에 마련해도 된다 — 자리부터 정하면 아직 없는 메뉴에 맞춰 코드를 먼저 짜게 된다.
- 문서 디자인(색·여백·타이포그래피)을 다시 만들지 않는다. 이미 있는 템플릿의 스타일 블록을 그대로 읽어 재사용하면 일관성이 저절로 유지된다.

**오늘 내린 결정**

- 제안 카드 3장을 만들면서 "하지 말 것"을 반드시 하나 이상 넣었다 — 없으면 아무것도 안 하기로 결정한 게 아니라 결정을 안 한 것이기 때문이다.
- "적용" 절의 후보는 판단기준.md의 가장 최근 "오늘 내린 결정"에서만 뽑기로 했다 — 문서 전체에서 아무 문장이나 뽑으면 "오늘 이 제안에 실제로 적용한 것"이라는 의미가 흐려진다.
- 오늘 어느 절을 자동으로, 어느 절을 사람으로 나눴는지:
  - 한 장 요약 — 자동. 카드·퍼널에서 그대로 뽑히는 사실이라서.
  - 1. 무엇을 발견했는가 — 자동. 다시 조회해도 같은 값이 나온다.
  - 2. 왜 이런 일이 생겼는가 — 자동. 축별로 쪼갠 결과를 그대로 옮긴 것이라서.
  - 3. 무엇을 하자고 제안하는가 — 자동. 카드에 이미 적힌 것을 옮기기만 했다.
  - 4. 이 제안이 틀린다면 — 사람. 무엇이 틀릴 수 있는지, 철회 조건은 판단이라서.
  - 5. 적용 — 사람(후보만 자동). 고르는 것은 판단이라서.

---

## 2026-09-14 — 9주차 Day3: 이 제안서를 결정권자 책상에 그대로 올릴 수 있는가

**배운 것**

- 분석 문서와 결재 문서는 읽는 사람이 다르다. 분석 문서는 "어떻게 계산했나"를 궁금해하는 사람이 읽고, 결재 문서는 "무엇을 해야 하나·얼마짜리인가·틀리면 어쩌나"를 궁금해하는 사람이 읽는다. 그래서 결재 문서에는 계산 과정·함수 이름 같은 것을 넣지 않는다 — 궁금하면 원본 도구를 열면 된다.
- 절의 순서가 곧 설득의 순서다. 무슨 일이 일어나는지 → 어디서 벌어지는지 → 얼마짜리 문제인지 → 그래서 뭘 하자는 건지 → 틀리면 어떻게 되는지 → 무엇을 해달라는 건지, 이 순서를 바꾸면 설득이 안 된다. 규모를 제안 뒤에 두면 "왜 해야 하는지"가 안 풀린 채로 제안을 읽고, 위험을 요청 뒤에 두면 결정한 다음에야 리스크를 듣게 된다.
- 절을 만들기 전에 "이 절을 채울 데이터 함수 이름을 지금 댈 수 있는가"부터 답한다. 못 대면 그 절은 없는 것이다. 나중에 채우겠다고 빈 자리를 만들어 두면 그 자리가 그대로 인쇄된다 — 자리를 비워 두는 것과 자리를 안 만드는 것은 다르다.
- 주제(발견) 하나로 문서 하나를 만들면, 그날 눈에 띈 것이 그대로 우선순위가 돼 버린다. 후보를 만들 수 있는 만큼 다 만들어 놓고 그중에서 고른다. 격차가 작아 기각된 후보도 목록에 남긴다 — 지우면 "안 봤다"와 "보고 아니었다"가 구분되지 않는다.
- 그림 하나에는 그 그림이 증명하는 문장 하나가 본문에 있어야 한다. 문장 없는 그림은 장식이다. 장식용 그래프(파이·3D·무지개색)는 아예 넣지 않는다. 색은 강조 1·대비 1·기본 1·회색 1, 그 이상 넘기지 않는다.
- 값이 없는 계열은 그리지 않는다. 0으로 그리면 "값이 0이다"와 "값이 없다"가 구분되지 않는다.
- 절 제목을 질문형으로 쓰면 그 아래 본문이 자동으로 그 질문에 대한 답이 된다 — "현황"보다 "지금 어디서 새고 있습니까"가 읽는 사람 입장에서 낫다.
- 사람이 쓰는 절은 되도록 둘로 제한한다. 셋 이상이면 자동화가 덜 된 것이고, 하나도 없으면 책임질 사람이 없는 문서다.
- 특정 주제를 보여줄 때는 그 주제와 관련된 재료만 보여준다. 관련 없는 재료(다른 주제의 카드 등)를 대신 보여주는 것보다, 없으면 "없다"고 정직하게 적는 편이 낫다.
- 문서 구조(절 목록)를 바꿀 때는 기존 템플릿을 참고하지 않고 내 데이터로 처음부터 다시 설계한다. 다만 이미 검증된 디자인 시스템(색·타이포그래피·인쇄 규칙)은 새로 만들지 않고 그대로 재사용한다 — 구조는 데이터에 맞게, 디자인은 일관되게.

**오늘 내린 결정**

- 주제 후보를 네 갈래(퍼널 구간·분해 축·임계값·추세)에서 뽑아 8개를 만들고, 그중 4개를 채택 가능으로 4개를 기각으로 남겼다. 기각 사유는 "구간 자체만으로는 손볼 곳을 특정할 수 없다"거나 "표본이 작아 우연 범위일 수 있다"거나 "관측 기간이 짧아 성과 문제인지 알 수 없다"였다.
- 사람이 쓰는 절을 "이 판단이 틀릴 수 있는 지점"과 "무엇을 결정해 주셔야 합니까" 둘로 확정했다. 나머지 네 절(요약·현황·원인·규모·제안)은 전부 데이터에서 조회되거나 카드에서 옮긴 사실이라 자동으로 채웠다.
- 주제와 무관한 카드를 대신 보여주지 않기로 했다 — 관련 카드가 없으면 빈 목록에 "이 주제에 대한 카드가 아직 없다"고 적기로 정했다. 상관없는 근거를 붙여 그럴듯해 보이게 만드는 것보다 정직한 게 낫다고 판단했다.
- 제안서를 리포트 화면의 임시 탭에서 독립 메뉴로 옮겼다. 어제(Day2) 탭을 "임시"라고 이름 붙인 이유가 오늘 드러났다 — 리포트와 제안서는 읽는 사람도 목적도 다른 문서라서 한 화면에 얹어 두면 안 됐다.

---

## 2026-09-14 — 9주차 Day4: 이 제안서로 결정이 나오는가

**배운 것**

- 읽히는 문서와 결정이 나오는 문서는 다르다. 점수(몇 점인지)는 행동으로 안 바뀐다 — 판정(채택/보류/기각)과 채택 조건("무엇이 있으면 채택하겠는가")이 그대로 다음에 할 일이 된다.
- 독자를 "일반적인 의사결정자"로 잡으면 아무 지적이나 나온다. 이 문서를 실제로 받을 사람의 역할·관심사·거절 사유를 구체적으로 정해야 지적이 날카로워진다.
- 같은 문서도 독자가 다르면 판정이 갈릴 수 있다. 그 갈리는 지점이 바로 문서의 약한 곳이다.
- 지적을 받으면 다 받아들이지 않는다. 먼저 셋으로 가른다 — 문서에 없는 것인가(고친다), 있는데 못 찾은 것인가(위치만 옮긴다, 내용을 새로 쓰지 않는다), 내 사정을 모르고 한 말인가(안 고친다). 둘째가 의외로 가장 많이 나오고 가장 자주 놓친다.
- 안 받아들인 지적에는 반드시 이유를 남긴다. 그 이유가 이 문서를 받을 사람에게 줄 실제 답변이 된다.
- 판정을 좋게 만들려고 확신도를 올리면 안 된다. 확인 계획("모른다 — 언제 어떻게 확인한다")은 약점이 아니라 문서의 신뢰다 — 지우거나 단정문으로 바꾸면 안 된다.
- 모른다고 답하는 것도 정당한 답변이다. 다만 "언제 알 수 있는가"가 반드시 붙어야 한다 — "모른다"로만 끝나면 답변이 아니다.
- 지적 중에는 실제로 데이터를 다시 조회해서 확인할 수 있는 것과, 아직 존재하지 않는 계획(비용 등)이라 지어낼 수 없는 것이 섞여 있다. 전자는 조회해서 채우고, 후자는 억지로 숫자를 만들지 않고 확인 계획인 채로 정직하게 남긴다.
- 벤치마크(남의 판정)는 문서가 잘 쓰였는지는 재지만, 제안 자체가 옳은지는 재지 못한다 — 근거 숫자가 맞는지는 그보다 앞서 이미 확인했어야 하는 것이고, 오늘은 그 근거를 남에게 설득력 있게 전달했는지만 본다.

**오늘 내린 결정**

- 페르소나 판정: **조건부 승인**. 사유는 "비용이 확인 필요로만 남아 실행 계획 없이 바로 시작하기엔 이르다"였고, 채택 조건은 "코칭 방식(누가·몇 회·몇 시간)이 구체화되면 즉시 승인"으로 남겼다.
- 지적 2건(A: 배정 편향 가능성 확인 안 됨, 잘못된 임계값 근거)은 실제 데이터를 다시 조회해 반영했다. 1건(B: 되돌림 가능 여부가 한 장 요약에 안 보임)은 위치만 옮겼다(요약에 바로 표시). 2건(C: 비용 미확인 상태 유지, 무관해 보이지만 실제로는 같은 축인 카드)은 안 받아들이고 이유를 남겼다 — 특히 비용은 실제 계획이 없는 상태에서 숫자를 지어내면 문서가 거짓이 되기 때문에 그대로 뒀다.
- 동료 지적 중 "실제 매출 기여 규모"는 실측 데이터(평균 계약 금액·실제 계약 기간)로 확인 가능해 계산해서 기록했지만, 이번 제안서 본문(주제 범위 밖)에는 넣지 않고 별도로만 남겼다 — 규모 절의 계산식 자체를 바꾸는 일이라 이번 개정 범위를 벗어난다고 판단했다.

---

## 2026-09-14 — 9주차 Day5: 벤치마크 피드백 반영 (트랙 B)

**배운 것**

- 외부 평가(내가 아닌 것)를 받으면 항목이 여러 개 나온다. 한 번에 다 고치려 하지 않고 가장 낮은 항목 하나만 고른다 — 나머지는 다음 라운드로 남긴다. 전부 고치면 무엇을 우선했는지가 사라진다.
- "효과가 미미하다"는 지적은 계산이 틀렸다는 뜻이 아니라 대상 범위가 좁았다는 뜻일 수 있다. 근거가 되는 대상을 실제로 넓혀 다시 계산해 보면 답이 나온다 — 숫자를 부풀리는 게 아니라 처음부터 셌어야 할 대상을 마저 세는 것이다.
- "실행 계획이 없다"는 지적 안에도 지어낼 수 있는 부분과 없는 부분이 섞여 있다. 담당 조직·확정 절차("누가 언제 정하는가")는 지금 정할 수 있어 채웠고, 정확한 비용·시수는 아직 존재하지 않는 계획이라 채우지 않았다 — 같은 지적이라도 항목을 쪼개서 가른다.
- 규모를 계산하는 공식 자체를 고치면(예: "최악의 사례 하나"에서 "기준 이하 전부"로), 그 공식을 공유하는 다른 후보의 값도 함께 바뀐다. 공식을 공유하게 해 둔 이유가 여기서 드러난다 — 한 곳만 고치면 전부 일관되게 갱신된다.
- 텍스트로 받은 근거 자료가 인코딩 손상으로 일부 복구 불가능할 수 있다. 프로그램으로 되돌리는 시도는 해볼 수 있지만, 정보가 실제로 사라졌다면 추측으로 채우지 않고 원본을 다시 요청한다 — 근거가 안 보이는 것과 근거를 지어내는 것은 다르다.

**오늘 내린 결정**

- "실행 가능성과 효과" 항목(최저 점수)만 골라 반영했다. 나머지 다섯 항목(문제 정의·데이터 근거·분석 연결·리스크·의사결정 요청)은 이번에 건드리지 않았다.
- 개선 대상을 표본이 충분한 담당자 중 최고를 제외한 나머지 전원으로 넓히고, 그 공식을 재사용되는 계산 함수 쪽에 반영해 같은 축을 쓰는 다른 후보에도 일관되게 적용되게 했다.
- 비용(구체적 시수) 산정 지적은 이번에도 받아들이지 않았다 — 실행 계획 자체가 아직 없어 숫자를 지어낼 근거가 없었다. 대신 담당 조직과 확정 절차는 지금 정할 수 있는 것이라 채웠다.
