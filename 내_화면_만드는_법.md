# 내 화면을 처음부터 만드는 법

**이 문서는 실습이 아니라 참고 자료입니다.** 8주차 중에 읽어도 되고, 수업이 끝난 뒤
자기 것을 만들 때 펴 봐도 됩니다.

---

# 0. 왜 골격에 맞추라고 했는가

8주차에 여러분은 **골격을 받아 자기 데이터를 맞췄습니다.** 이런 의문이 들 수 있습니다.

> *"내가 왜 남이 만든 틀에 내 데이터를 갖다 맞춰야 하지?"*

**편차를 없애기 위해서입니다.**

| | 0에서 만들면 | 골격에서 시작하면 |
|---|---|---|
| 5일 뒤 결과 | 사람마다 천차만별 | **전원이 같은 수준** |
| 시간을 쓰는 곳 | 레이아웃 · 색 · 차트 그리기 | **판단과 계산** |
| 막혔을 때 | 물어볼 기준이 없다 | 정답지가 있다 |
| 화면 품질 | 운에 달림 | **바닥이 보장됨** |

실무에서도 빈 파일에서 대시보드를 시작하지 않습니다. 5일 안에 0에서 만들면
화려할 수 없고, **정작 배워야 할 판단에 시간을 못 씁니다.**

## 그리고 이 문서는 그 다음을 다룹니다

골격은 출발점이지 종착점이 아닙니다. **여러분도 자기 주제로 처음부터 만들 수 있습니다.**
그 골격을 만든 것도 결국 사람과 Claude Code의 대화였습니다.

다만 두 가지는 그대로 갑니다.

| | |
|---|---|
| **품질** | 아무거나가 아니라 **남에게 보여줄 수 있는 수준** |
| **범위** | **데이터를 받아 리포트 자동화까지** |

> **이 문서에는 "이대로 치세요" 하는 프롬프트가 없습니다.**
> 좋은 화면은 정형화된 주문으로 나오지 않습니다. **어떤 순서로 무엇을 주며 대화하는가**가
> 전부입니다. 그 방법을 다룹니다.

---

# 1. 품질은 어디서 오는가

여러분이 5일간 쓴 골격을 뜯어보면, 괜찮아 보이는 이유가 **다섯 가지**입니다.
전부 도메인과 무관합니다.

| # | 무엇 | 이게 없으면 |
|---|---|---|
| **1** | **명세를 먼저 썼다** | 대화할 때마다 즉흥이 되고, 다음 대화에서 갈아엎는다 |
| 2 | 색·간격·글자 크기가 **한 파일에** 있다 | 한 곳을 고쳐도 다른 화면이 안 따라온다 |
| 3 | 화면을 **성격으로** 나눴다 | 한 화면에 다 넣어서 아무것도 안 보인다 |
| 4 | **바꾸면 안 되는 것**을 정해뒀다 | 판정을 전달하던 색이 어느새 장식이 된다 |
| 5 | **돌려보고 고친 것을 기록**했다 | 같은 함정에 두 번 빠진다 |

## 1번이 열쇠입니다

나머지 넷도 결국 **명세에 적혀 있었기 때문에** 지켜졌습니다.

여러분이 8주차 내내 참조한 `DESIGN.md` 가 그 명세입니다. 그 문서가 먼저 있었기 때문에
색이 흩어지지 않았고, 화면이 섞이지 않았고, 고정 조항이 지켜졌습니다.

```
명세가 없으면        →  대화 1: "이렇게 만들어줘"     →  뭔가 나온다
                       대화 2: "아 이건 아니고..."    →  갈아엎는다
                       대화 3: "아까 그게 나았는데"   →  되돌린다

명세가 있으면        →  명세를 고친다  →  코드가 따라온다
```

**명세 단계에서 고치면 한 줄이고, 코드가 나온 뒤에 고치면 하루입니다.**

> 세부 규칙(색 값·간격 배수·`tabular-nums` 같은 것)은 `DESIGN.md` 3절에 있습니다.
> 여기서 다시 쓰지 않겠습니다. **여기서 다루는 것은 그런 명세를 어떻게 얻느냐**입니다.

---

# 2. Claude에게 주기 전에 내가 정할 것

**정하지 않고 시키면 Claude가 대신 정합니다.** 그러면 내 화면이 아닙니다.

| 정할 것 | 안 정하면 |
|---|---|
| **데이터** — 한 행이 무엇인가 · 기간 · 컬럼 | 엉뚱한 축으로 집계합니다 |
| **누구에게 무엇을 보고하는가** | 화면이 백화점이 됩니다 |
| **그 사람이 무슨 결정을 내리는가** | 예쁜데 쓸모없는 대시보드가 나옵니다 |
| **몇 번 보는 화면인가** | 한 번 쓰는 것과 매일 보는 것이 섞입니다 |
| **바꾸면 안 되는 것** | 다음 대화에서 갈아엎습니다 |

## 세 번째가 가장 자주 빠집니다

"매출 대시보드를 만들어줘"는 **무엇을 결정하려는지가 없습니다.**

```
✕   매출 현황을 볼 수 있는 대시보드
○   이번 분기에 어느 채널에 예산을 더 넣을지 정하려고 한다.
     그 판단에 필요한 것만 보여달라.
```

아래쪽으로 물으면 **화면에 안 들어갈 것이 정해집니다.** 그게 더 중요합니다.

## 8주차 수강생은 이미 갖고 있습니다

7주차에 만든 **명세**가 정확히 이것입니다.

| 명세 | 여기서 쓰는 것 |
|---|---|
| 데이터 (테이블·기간) | 위 표 1행 |
| 퍼널 단계 | 화면의 뼈대 |
| 주지표 + 가드레일 | 무엇을 보여줄 것인가 |
| 임계값 | 상태 색 기준 |
| 유지 / 이탈 정의 | 뒷단 화면 |

**새 주제로 갈 때도 이 다섯 줄을 먼저 채우십시오.** 순서가 같습니다.

---

# 3. 디자인 수준을 전달하는 법

"예쁘게 만들어줘"로는 안 나옵니다. 취향이 매번 달라집니다.

**세 가지 중 하나로 전달합니다.**

| 방법 | 어떻게 말하나 | 언제 |
|---|---|---|
| **참조를 준다** | *"이 앱의 `DESIGN.md` 3절과 같은 수준으로"* | **가장 정확합니다** |
| **용도로 말한다** | *"임원 보고에 그대로 띄울 수 있는 수준"* / *"팀에서 매일 보는 내부용"* | 참조가 없을 때 |
| **요소로 지정한다** | 색 팔레트 · 여백 밀도 · 폰트 · 차트 스타일 | 브랜드가 정해져 있을 때 |

## 참조를 주는 것이 가장 강력합니다

여러분에게는 이미 참조가 있습니다. **`DESIGN.md` 를 그대로 건네십시오.**

```
첨부한 DESIGN.md 의 3절(디자인 시스템)과 같은 수준으로 만들어줘.
색 의미·간격 규칙·숫자 표기는 그대로 따르고,
브랜드 색만 [ 내 색 ] 으로 바꿔줘.
```

이 한 문단이 "예쁘게 해줘" 열 번보다 낫습니다.

## 그리고 밀도와 톤을 한 줄로

같은 수준이어도 인상이 갈립니다.

| 축 | 한쪽 | 반대쪽 |
|---|---|---|
| 밀도 | 한 화면에 많이 (조밀) | 여백 크게 (한 번에 하나) |
| 톤 | 차분함 (회색·남색 위주) | 강렬함 (대비 큰 색) |
| 숫자 | 크게 앞세움 | 문장 속에 녹임 |

```
밀도는 조밀하게. 한 화면에서 스크롤 없이 판단이 끝나야 한다.
톤은 차분하게. 강조 색은 판정에만 쓰고 장식에는 쓰지 마.
```

> **판정에 쓰는 색과 장식에 쓰는 색을 나누는 것**이 품질을 가장 크게 가릅니다.
> 빨강이 "위험"을 뜻하는 화면에서 제목도 빨강이면, 빨강은 아무 뜻도 없게 됩니다.

---

# 4. 대화의 순서 — 네 단계

**여기가 이 문서의 본체입니다.**

한 번에 "앱 만들어줘"라고 하면 즉흥적인 것이 나옵니다. **네 단계로 나눕니다.**

```
①  명세를 쓰게 한다          코드는 아직 쓰지 마
②  명세를 읽고 고친다        ← 여기가 제일 중요
③  화면을 하나씩 만든다      한 번에 넷을 만들면 어디가 틀렸는지 모른다
④  돌려보고, 고친 것을       명세에 되돌린다
```

---

## ① 명세를 쓰게 합니다

**코드를 쓰지 말라고 명시적으로 말하십시오.** 안 그러면 바로 코드가 나옵니다.

```
내가 만들려는 것

  주제      [ 무엇을 분석하는가 ]
  대상      [ 누구에게 보고하는가 ]
  결정      [ 그 사람이 무슨 결정을 내리는가 ]

내 데이터

  [ 파일 이름 ] — 한 행이 [ 무엇 ] 이다. [ N ] 행. 컬럼: [ ... ]
  [ 파일 이름 ] — ...
  기간: [ 시작 ~ 끝 ]

내가 이미 정한 것

  주지표     [ 이름 ] — 현재값 [ 값 ]
  가드레일   [ 이름 ]
  임계값     경고 [ 값 ] / 위험 [ 값 ]

────────────────────────────────────────
코드는 아직 쓰지 마. 먼저 설계 명세를 문서로 써줘.

- 화면을 몇 개로 나눌 것인가. 각 화면은 누가 언제 보는가
- 각 화면에 무엇이 올라가는가
- 무엇을 바꾸면 안 되는가 (고정 조항)
- 내가 결정해야 할 것이 무엇인지 목록으로 뽑아줘   ★
```

**마지막 줄이 중요합니다.** Claude가 임의로 정해버릴 것들을 **결정 목록으로 끌어올립니다.**

> 이 골격의 설계안에도 **「승인이 필요한 판단 5가지」** 라는 절이 있습니다.
> 만들기 전에 사람이 정할 것을 먼저 뽑아낸 것입니다.

---

## ② 명세를 읽고 고칩니다 ★

**이 단계를 건너뛰면 나머지가 다 흔들립니다.**

나온 명세를 읽으면서 이렇게 봅니다.

| 볼 것 | 아니면 |
|---|---|
| 내가 안 정한 것을 **Claude가 정해버렸는가** | 그 자리를 되돌려 받는다 |
| **화면에 안 들어갈 것**이 정해졌는가 | 다 넣으려 하고 있다 |
| 화면마다 **보는 빈도**가 다른가 | 성격이 섞였다 |
| 고정 조항이 **실제로 지켜질 수 있는 것**인가 | 지킬 수 없는 규칙은 안 지켜진다 |

고칠 때는 이렇게 말합니다.

```
명세를 읽었다. 세 군데를 고쳐줘.

1. [ 항목 ] 은 내가 정한다. [ 내 결정 ] 으로 바꿔줘
2. [ 화면 ] 에 [ 요소 ] 는 빼줘. [ 이유 ]
3. 고정 조항에 [ 규칙 ] 을 추가해줘. [ 이유 ]

고친 명세를 파일로 저장해줘. 앞으로 이 파일이 기준이다.
```

**"앞으로 이 파일이 기준이다"를 반드시 말하십시오.** 이 한 줄이 다음 대화들을 묶어줍니다.

---

## ③ 화면을 하나씩 만듭니다

**한 번에 넷을 만들면 어디가 틀렸는지 모릅니다.**

순서는 **데이터에 가까운 것부터**입니다.

```
1.  데이터를 읽고 검증하는 화면      ← 여기가 틀리면 나머지가 다 틀린다
2.  계산해서 보여주는 화면
3.  문서로 내보내는 화면
4.  지난 것을 찾는 화면
```

한 화면을 시킬 때는 이렇게 합니다.

```
명세의 [ 화면 이름 ] 을 만들어줘.

- 명세에 적힌 요소만. 명세에 없는 건 넣지 마
- 계산은 [ 파일 ] 에, 화면은 [ 파일 ] 에 나눠서
- 색과 간격은 [ 파일 ] 에서만 정의하고 여기서는 가져다 써

다 만들면 어떻게 실행하는지 알려줘.
```

> **"명세에 없는 건 넣지 마"** 를 넣는 이유가 있습니다. 안 그러면 친절하게
> 이것저것 더 붙여 주는데, 그게 쌓이면 명세와 화면이 어긋납니다.

**하나가 끝나면 반드시 실행해 봅니다.** 다음 화면으로 넘어가기 전에요.

---

## ④ 돌려보고, 고친 것을 명세에 되돌립니다

실행하면 반드시 뭔가 안 됩니다. **그게 정상입니다.**

고치고 나서 **한 가지를 더 해야 합니다.**

```
방금 고친 것들을 명세에 반영해줘.

- 왜 그렇게 고쳤는지 한 줄씩
- 다시 만들 때 같은 문제를 안 겪게 "흔히 겪는 함정" 절에 추가
```

**이걸 안 하면 다음 대화에서 되돌아갑니다.** Claude는 명세를 보고 만드니까요.

> 여러분이 쓴 `DESIGN.md` 의 7절 「흔히 겪는 함정」이 정확히 그렇게 쌓인 것입니다.
> 한글 폰트가 깨진 것, 메모리가 넘친 것, 타입 변환에서 터진 것 —
> **전부 겪고 나서 적은 것**입니다.

---

## 네 단계를 한 장으로

| 단계 | 무엇을 준다 | 무엇을 받는다 | 걸리는 시간 |
|---|---|---|---|
| ① | 데이터·대상·결정·이미 정한 것 | **명세 초안** + 결정할 것 목록 | 10분 |
| ② | 내 결정 · 뺄 것 · 고정 조항 | **확정 명세** (파일) | 20분 ← 제일 중요 |
| ③ | 명세의 화면 하나 | 동작하는 화면 하나 | 화면당 20~40분 |
| ④ | 실행하며 만난 문제 | 고친 코드 + **갱신된 명세** | 화면당 10분 |

**②에 시간을 가장 많이 쓰십시오.** 여기서 20분을 아끼면 나중에 하루를 씁니다.

---

# 5. 리포트 자동화까지 가려면

화면만 만들고 끝내지 않습니다. **데이터를 받아 문서가 나오는 것**까지가 한 벌입니다.

도메인이 바뀌어도 **이 네 가지는 같습니다.**

| | 왜 |
|---|---|
| 문서 여러 장 중 **일부는 사람이 쓴다** | 해석과 제안을 자동화하면 **책임이 사라진다** |
| 자동 문장의 **인과 표현을 검사한다** | 관측 데이터로는 인과를 주장할 수 없다 |
| **한계는 검증 경고에서 조립한다** | 경고가 났는데 한계에 없으면 사라진 것과 같다 |
| **계산에 현재 시각을 쓰지 않는다** | 같은 입력에 같은 결과가 나와야 한다 |

명세를 쓸 때 이 넷을 **고정 조항으로 넣으십시오.**

```
고정 조항에 이걸 넣어줘.

- 해석과 제안은 자동 생성하지 않는다. 사람이 쓰는 자리로 남긴다
- 자동 생성 문장에 인과를 단정하는 표현이 있으면 경고한다
- 검증에서 경고가 난 항목은 문서의 한계 절로 옮긴다
- 계산 경로에 현재 시각을 넣지 않는다
```

## 왜 "주제를 입력하면 알아서 계산해주는" 화면은 안 만드는가

이런 생각이 들 수 있습니다.

> *"화면에서 내가 보고 싶은 주제를 쓰면, AI가 알아서 계산해주면 안 되나?"*

**기술적으로는 됩니다. 그런데 이 구조와 충돌합니다.**

```
같은 데이터로 같은 주제를 두 번 물었다
   →  계산식이 조금 다르게 만들어졌다
   →  결과가 다르게 나왔다
   →  어느 쪽이 맞는가?  아무도 답할 수 없다
```

**재현되지 않는 숫자는 보고에 쓸 수 없습니다.** 위에서 "계산에 현재 시각을 쓰지 않는다"를
고정 조항으로 둔 것과 같은 이유입니다.

굳이 하려면 **사람이 승인하는 자리**를 넣어야 합니다 — 주제를 받아 *무엇을 계산할지*
제안하게 하고, 사람이 확인하고, **확정된 계산식을 기록에 남기는** 구조입니다.
그러면 재현이 됩니다. **다만 그건 이 수업 범위 밖입니다.**

---

# 6. 이 골격은 실제로 이렇게 만들어졌습니다

여러분이 쓴 앱이 만들어진 순서입니다. **위 네 단계가 실제로 어떻게 돌아갔는지** 보여줍니다.

| 순서 | 무엇을 | 사람이 정한 것 | Claude가 한 것 | 남은 파일 |
|---|---|---|---|---|
| 1 | **기획** | 무엇을 왜 만드는가 · 문제의식 · 대전제 | 문서 정리 | `프로젝트3_기획.md` |
| 2 | **데이터 설계** | 규모 · 기간 · **심을 함정** | 테이블 구성 초안 | `데이터셋_설계안.md` |
| 3 | **생성 · 실측** | — | 데이터 생성 · 실제 값 측정 | `실측결과.md` |
| 4 | **화면 설계** | 화면 개수 · 각 화면의 성격 | 요소 배치 초안 | `화면_설계안.md` |
| 5 | **제작** | 승인 · 고칠 것 지시 | 코드 | 앱 |
| 6 | **명세화** | 무엇을 고정할 것인가 | 문서화 | `DESIGN.md` |

## 눈여겨볼 것 셋

**① 코드는 5번에서 처음 나옵니다.** 1~4는 전부 문서입니다.

**② 2번과 4번 설계안에는 「승인이 필요한 판단」 절이 있습니다.**
데이터 설계안에 4가지, 화면 설계안에 5가지. **만들기 전에 사람이 정할 것을 먼저 뽑았습니다.**

**③ 3번(실측)이 4번(화면 설계)보다 먼저입니다.**

> 실험 결과가 *"상대 +18.3%, 신뢰구간 [+4.1%, +33.2%]"* 처럼 **실제 값으로 나와야**
> 카드에 무엇을 넣을지 정해집니다. 가짜 숫자로 화면을 설계하면 실제 데이터를 넣는 순간
> 레이아웃이 깨집니다.

**여러분도 같은 순서로 하십시오.** 데이터를 먼저 보고, 실제 숫자를 확인한 다음, 화면을 설계합니다.

---

# 7. 흔히 하는 실수

| 실수 | 왜 문제인가 | 대신 |
|---|---|---|
| 한 번에 **"앱 만들어줘"** | 즉흥으로 나오고 고칠 기준이 없다 | 명세부터 (4절 ①) |
| **명세 없이 코드부터** | 두 번째 대화에서 갈아엎는다 | 코드를 쓰지 말라고 명시 |
| **화면 넷을 동시에** | 어디가 틀렸는지 모른다 | 하나씩, 실행해 보고 다음 |
| **"예쁘게 해줘"** | 취향이 매번 바뀐다 | 참조를 주거나 용도로 말한다 (3절) |
| **돌려보지 않고** 다음으로 | 안 되는 걸 위에 쌓는다 | 화면마다 실행 |
| 고친 것을 **명세에 안 남김** | 다음 대화에서 되돌아간다 | 4절 ④ |
| **가짜 숫자로 화면 설계** | 실제 데이터에서 레이아웃이 깨진다 | 실측 먼저 (6절) |
| 판정 색을 **장식에도 씀** | 색이 아무 뜻도 없게 된다 | 강조는 판정에만 |

## 가장 흔한 하나

**"이것도 넣어주세요"를 반복하는 것**입니다.

한 번에 하나씩은 다 합리적으로 보이는데, 열 번 쌓이면 처음 정한 것과 전혀 다른 화면이
되어 있습니다. 그리고 **명세에는 그 열 개가 없습니다.**

```
추가하고 싶은 것이 생기면
   →  먼저 명세에 넣는다
   →  명세에서 봤을 때도 말이 되면 그때 만든다
```

명세에서 봤을 때 이상하면, 화면에서는 더 이상합니다.

---

# 한 장 요약

```
품질은 명세에서 온다. 명세 없이 만들면 대화할 때마다 갈아엎는다.

Claude에게 주기 전에 넷을 정한다 —
   데이터가 무엇인지 · 누구에게 보고하는지 ·
   그 사람이 무슨 결정을 내리는지 · 무엇을 바꾸면 안 되는지.

디자인 수준은 참조로 전달한다. "예쁘게"로는 안 나온다.

네 단계로 대화한다 —
   ① 명세를 쓰게 하고  ② 읽고 고치고  ③ 하나씩 만들고  ④ 고친 것을 명세에 되돌린다.
   ②에 시간을 가장 많이 쓴다.

실제 숫자를 먼저 보고 화면을 설계한다. 가짜 숫자로 설계하면 나중에 깨진다.

추가하고 싶은 것은 명세에 먼저 넣는다.
   명세에서 이상하면 화면에서는 더 이상하다.
```
