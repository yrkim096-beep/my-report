---
description: 분석 결과에서 제안 후보를 뽑아 제안 카드로 만듭니다. 근거를 데이터에서 실제로 조회하고, 근거 없는 제안은 차단합니다
argument-hint: <앱 폴더 또는 리포트 경로> (비워두면 현재 폴더에서 찾음)
---

# 제안

대상: **$ARGUMENTS**

---

## 0. 이 명령의 성격

8주차에 만든 앱과 리포트에서 **제안 후보를 뽑아 카드로 만든다.**
9주차 화요일에 처음 쓰고, **수·금에 같은 파일을 이어서 채운다.**
(목요일은 카드를 안 채운다 — 카드를 읽어 **제안서**를 만드는 날이다.)

| 한다 | 하지 않는다 |
|---|---|
| 후보를 3분류로 나눠 제시 | **어느 것을 할지 정하기** |
| 근거 숫자를 **데이터에서 실제로 조회** | 리포트 문장을 근거인 척 옮기기 |
| 비용·효과·되돌림을 **물음** | 모르는 값을 그럴듯하게 채우기 |
| 근거 없는 제안을 **차단** | 일단 카드부터 만들어 두기 |
| 순서 기준을 알려줌 | **순서를 대신 매기기** |

> **제안은 선택이고, 선택은 사람이 한다.** 이 명령은 선택할 재료를 갖춰 놓을 뿐이다.
> 무엇을 하지 말자고 할 것인지는 대신 정해줄 수 없다.

---

## 0-1. 대상 찾기

```
1. $ARGUMENTS 가 주어졌으면 그것을 쓴다
2. 없으면 현재 폴더에 app.py 또는 report/ 가 있는지 본다
3. 없으면 하위 한 단계에서 찾는다
4. 그래도 없으면 경로를 물어본다. 추측하지 않는다
```

**아래 셋이 다 있어야 시작한다.**

| 필요한 것 | 없으면 |
|---|---|
| 계산된 지표 (앱이 돌아간다) | **중단.** 8주차를 먼저 끝내야 한다 |
| 검증·게이트 기록 | 중단. 근거의 신뢰도를 알 수 없다 |
| 리포트 7장(한계) | 경고하고 진행. 단 모든 카드가 "추정" 이하다 |

기억이나 대화 내용으로 대신하지 않는다. **파일에 없으면 없는 것이다.**

---

## 1. 후보를 3분류로 뽑는다

```
하지 말 것    지금 하고 있는데 근거가 없거나 해로운 것
다시 할 것    판정이 무효였거나 표본이 모자랐던 것
할 것        근거가 있고 효과를 추정할 수 있는 것
```

**"하지 말 것"부터 뽑는다.** 순서를 바꾸지 않는다. 사람은 "할 것"을 먼저 쓰기 시작하면
거기서 멈추고, 멈추는 제안은 영영 나오지 않는다.

### 어디서 뽑는가

| 분류 | 재료 |
|---|---|
| **하지 말 것** | 가드레일이 악화된 판정 · 효과가 사라진 항목 · 근거 없이 유지 중인 것 |
| **다시 할 것** | **무효 판정** · 표본 부족으로 감춰진 항목 · 한계 절의 "확인 못 함" |
| **할 것** | 격차가 크고 손을 쓸 수 있는 분해 결과 · 성공 판정 |

**감춰진 항목을 "할 것"에 넣지 않는다.** 감춘 것은 아직 모르는 것이다.
모르는 것은 **"다시 할 것"** 으로 간다.

---

## 2. 근거를 실제로 조회한다 ★

**이 절이 이 명령의 핵심이다.**

각 후보마다 근거가 되는 숫자를 **데이터에서 직접 조회한다.** 리포트에 적힌 문장을
그대로 옮기지 않는다. 리포트가 틀렸을 수도 있고, 기간이 달라졌을 수도 있다.

```
후보:  모바일 신청 절차를 고친다
        ↓
조회:  기기별 신청 시작 -> 완료 건수와 비율
        ↓
근거:  모바일 35.9% (13,472 -> 4,835) vs 데스크톱 55.2%  격차 19.3%p
        비중 70.4%
```

**근거에 반드시 들어가야 하는 셋.**

| | 없으면 |
|---|---|
| **분자와 분모의 실제 건수** | 비율만으로는 표본을 알 수 없다 |
| **비교 대상** | "낮다"는 무엇에 비해 낮은가 |
| **비중** | 작은 칸을 고쳐도 전체는 안 움직인다 |

### 조회하지 못했으면

**"미확인"이라고 적는다.** 값을 지어내지 않고, 리포트에서 옮겨 적지도 않는다.

```
근거    미확인 — [ 무엇을 ] 조회하지 못했다. [ 왜 ]
```

미확인인 채로도 카드는 남긴다. **다만 "할 것"에는 못 들어간다.** ->§4

---

## 3. 넷을 묻는다 — 대신 채우지 않는다

제안 하나가 갖춰야 할 넷이다. **모르는 것은 "미확인"으로 남기고 사용자에게 묻는다.**

| | 물을 것 | 대신 채우면 |
|---|---|---|
| **근거** | 어느 숫자에서 나왔는가 | 의견이 사실이 된다 |
| **비용** | 무엇이 드는가 — 돈·사람·시간 | 결정할 수 없는 제안이 된다 |
| **효과** | 얼마나 달라지는가 | **가장 자주 지어낸다.** 아래 참고 |
| **되돌림** | 잘못됐을 때 되돌릴 수 있는가 | 시도조차 못 한다 |

### 효과를 계산할 때 지킬 것

```
관측된 격차                       = 실측
격차의 일부가 좁혀진다고 가정한 값   = 추정.  가정을 함께 적는다
```

**둘을 한 숫자로 합치지 않는다.** 화면·문서에 쓸 때 어느 쪽인지 라벨을 단다.

> 8주차 DESIGN.md §4-3 *"가정값과 실측값을 섞지 마십시오"* 와 같은 규칙이다.
> 여기서 섞으면 제안서 전체가 실측처럼 읽힌다.

### 비용을 모를 때

사용자가 모르면 **묻고 멈춘다.** 업계 평균이나 짐작을 넣지 않는다.

```
비용    미확인 — 개발 공수를 확인해야 한다
```

**미확인인 비용은 정직한 카드다.** 지어낸 비용은 틀린 카드다.

---

## 4. 차단 조건 — 걸리면 파일을 쓰지 않는다

| # | 조건 | 왜 |
|---|---|---|
| 1 | **근거가 데이터에 없다** | 제안이 아니라 의견이다 |
| 2 | **"하지 말 것"이 하나도 없다** | **선택을 안 한 것이다** |
| 3 | 효과를 확인 못 했는데 "할 것"에 넣었다 | 확인 없이 결론 낸 것이다 |
| 4 | 근거가 **감춰진(못 믿을) 항목**이다 | 화면에서 감춘 것을 제안에 쓰면 감춘 의미가 없다 |

**차단이면 무엇을 고쳐야 하는지만 알린다.** 파일은 만들지 않는다.

### 2번을 완화하지 않는다

*"저희 조직은 지금 하는 게 다 맞아서요"* 라는 답이 오면 이렇게 되묻는다.

```
근거를 대고 하고 있는 것이 몇 개입니까.
근거 없이 관성으로 하고 있는 것은 몇 개입니까.
```

**둘째가 0이면 그것부터 확인해야 한다.** 완화 대상이 아니다.

---

## 5. 카드 파일

`my-report/제안카드.md` 에 쓴다. **덮어쓰지 않는다.** 이미 있으면 아래에 이어 붙이거나,
같은 제안이면 **그 카드 안의 빈 칸만** 채운다.

```markdown
# 제안 카드

작성 시작: <날짜>   최종 갱신: <날짜>

---

## 제안 1 — <한 줄. 동사로 끝난다>

| | |
|---|---|
| **분류** | 하지 말 것 / 다시 할 것 / 할 것 |
| **근거** | <분자·분모 실제 건수 · 비교 대상 · 비중> |
| **비용** | <또는 미확인 — 왜> |
| **효과** | <실측 / 추정(가정 명시) / 미확인> |
| **되돌림** | 가능 <어떻게> / 불가 <왜> |

### 반증                                     <- 수요일에 채운다
이 제안이 틀렸다면 <  > 때문일 것이다.
확인하려면 <  > 을 보면 된다.

| 확인 결과 | |
|---|---|
| 무엇을 봤는가 | |
| 뒤집혔는가 | |
| **확신도** | 확인됨 / 추정 / 미확인 |

### 받은 반박                                 <- 금요일에 채운다
| | |
|---|---|
| 무엇을 지적받았나 | |
| 어떻게 처리했나 | 확인함 / 배제 못 함으로 추가 / 안 받아들임 |
| **안 받아들였다면 왜** | **비우지 않는다.** 토요일 심의 답변이 여기서 나온다 |
| 확신도가 바뀌었나 | |

---
```

**칸을 지우지 않는다.** 아직 못 채운 칸은 비워 두면 무엇이 남았는지 보인다.

---

## 6. 순서는 사용자가 매긴다

기준만 알려주고 **대신 정렬하지 않는다.**

```
효과 x 확실성 / 비용
되돌릴 수 있는 것을 먼저
```

| 물어볼 것 | |
|---|---|
| 되돌릴 수 없는 것이 위에 있는가 | 확신도가 "확인됨"이 아니면 내린다 |
| 확신도가 낮은데 비용이 큰 것이 있는가 | **다시 할 것**으로 옮길 후보다 |
| "하지 말 것"이 맨 아래에 있는가 | 멈추는 것이 가장 싸다. 왜 아래인지 물어본다 |

---

## 7. 결과 보고 형식

```
# 제안 카드 — <경로>

조회 일시: <실행 시각>
근거 조회: 실측 N건 / 미확인 N건

## 분류별

  하지 말 것   N건
  다시 할 것   N건
  할 것        N건

## 채워지지 않은 칸

| 제안 | 빈 칸 | 무엇을 확인해야 하는가 |
|---|---|---|

## 사용자가 정해야 할 것

| # | 무엇 | 왜 내가 못 정하는가 |
|---|---|---|
```

**차단이면 맨 위에 이렇게 적는다.**

```
카드를 만들지 않았습니다. 차단 N건을 먼저 보십시오.
```

---

## 8. 자체 검사 — 보고하기 전에

| 확인 | |
|---|---|
| 근거 숫자를 **실제로 조회**했는가 | 리포트에서 옮겼으면 그건 실측이 아니다 |
| 조회 못 한 것을 **미확인**으로 적었는가 | |
| 실측과 추정에 **라벨**이 붙었는가 | |
| **"하지 말 것"이 있는가** | 없으면 차단이다 |
| 감춰진 항목을 근거로 쓰지 않았는가 | |
| 비용·효과를 **대신 채우지 않았는가** | |
| **순서를 대신 매기지 않았는가** | |
| 기존 카드를 **덮어쓰지 않았는가** | 빈 칸만 채웠는가 |

**마지막에서 둘째가 가장 자주 깨진다.** 후보를 나열하면 순서가 생긴 것처럼 보인다.
순서를 뜻하지 않는다는 것을 보고에 적는다.
